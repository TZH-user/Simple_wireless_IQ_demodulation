# 恒温晶振更换后复测清单

## 目的

区分三件事：

1. 硬件频率稳定性是否真的改善
2. 现有载波锁定修正是否还适用
3. 现有解调/参数识别代码是否还要改

## A. DDS 时钟前提确认

需要确认：

- AD9959 实际 `SYSCLK` 是否仍然是 `500MHz`
- 只是参考更稳，还是频率链本身也改了

去哪里看：

- 硬件原理图
- AD9959 参考时钟/PLL 配置
- [ad9959.c](D:/VSCodeCMAKEProject/Simple_wireless_IQ_demodulation/Core/App/DDS_9959/Hardware/ad9959.c:263)

用来判断：

- 如果 DDS 实际 `SYSCLK` 仍是 `500MHz`，这段代码先不改
- 如果不再是 `500MHz`，必须先改 DDS 频率换算，再做后续测试

## B. 载波锁定误差复测

测试点：

- `30MHz`
- `35MHz`
- `40MHz`
- `45MHz`
- `50MHz`

每个点建议：

- `CW`
- `1.0VPP` 或 `1.5VPP`
- 每点至少 `5` 次

要看什么：

- 屏幕显示载波频率
- 串口 `sigdet_final`
- 如果方便，记录 DDS 实际设定值

去哪里看：

- 屏幕载波显示
- 串口日志中的 `sigdet_final`
- [app_signal_detect.c](D:/VSCodeCMAKEProject/Simple_wireless_IQ_demodulation/Core/App/Signal/app_signal_detect.c)

用来判断：

- 现有锁定修正是否仍然正确
- 是否还存在固定偏差
- 是否需要重新调整扫频修正策略

判据：

- 如果 5 个频点都基本稳定落在可接受误差内，先不改锁定代码
- 如果整体出现新的固定偏移，再改锁定修正

## C. 零中频残余差频观察

测试场景：

- 选一个中间频点，例如 `40MHz CW`

要看什么：

- I/Q 基带在示波器上的低频残余
- 解调页波形是否还带缓慢起伏包络

去哪里看：

- 示波器看 `PC4(I)`、`PB1(Q)`
- 解调页 AM 波形

用来判断：

- 恒温晶振是否明显压低 `RF-LO` 残余差频
- 之前 AM 解调的“低频起伏包络”是否进一步减轻

判据：

- 如果残余差频明显下降，`app_demod_am.c` 一般不用改
- 如果仍然明显，问题不只在参考稳定度，还可能有 I/Q 不平衡或锁定误差

## D. AM 解调页复测

测试信号：

- `40MHz`
- `AM 10kHz`
- `50% depth`
- `1.0VPP` 或 `1.5VPP`

要看什么：

- 解调页波形是否更平稳
- 一周期点数是否更接近理论值
- 低频起伏包络是否进一步减轻

去哪里看：

- 解调页
- [app_demod_am.c](D:/VSCodeCMAKEProject/Simple_wireless_IQ_demodulation/Core/App/Signal/Demod/app_demod_am.c)

用来判断：

- 当前 AM 包络算法是否已经足够
- 是否还需要进一步做平滑或 I/Q 校正

判据：

- 如果波形明显更干净，AM 解调代码先不动
- 如果仍有明显慢包络，再考虑二次优化

## E. `Rate(est)` 复测

测试信号：

- `AM 10kHz`
- 也可再测 `5kHz / 20kHz`

要看什么：

- 解调页 `Rate(est)`
- 识别页参数区 `Mod Rate`

去哪里看：

- 屏幕
- [app_mod_detect.c](D:/VSCodeCMAKEProject/Simple_wireless_IQ_demodulation/Core/App/Signal/app_mod_detect.c)

用来判断：

- 参数识别算法误差是否主要来自残余差频，还是算法本身缩放/边沿计数就不准

判据：

- 如果换恒温晶振后仍从 `10kHz` 读成 `7.9kHz` 一类值，说明这块仍是算法问题，不是时钟稳定度问题

## F. 是否需要改代码的决策表

1. DDS `SYSCLK` 没变，只是更稳
- 先不改 [ad9959.c](D:/VSCodeCMAKEProject/Simple_wireless_IQ_demodulation/Core/App/DDS_9959/Hardware/ad9959.c)

2. 锁定误差复测仍然准确
- 先不改 [app_signal_detect.c](D:/VSCodeCMAKEProject/Simple_wireless_IQ_demodulation/Core/App/Signal/app_signal_detect.c)

3. AM 波形更干净、低频起伏更轻
- 先不改 [app_demod_am.c](D:/VSCodeCMAKEProject/Simple_wireless_IQ_demodulation/Core/App/Signal/Demod/app_demod_am.c)

4. `Rate(est)` 仍明显偏差
- 继续改 [app_mod_detect.c](D:/VSCodeCMAKEProject/Simple_wireless_IQ_demodulation/Core/App/Signal/app_mod_detect.c)

5. DDS 实际系统时钟变了
- 必改 [ad9959.c](D:/VSCodeCMAKEProject/Simple_wireless_IQ_demodulation/Core/App/DDS_9959/Hardware/ad9959.c:263)

## 最小执行顺序

1. 先确认 DDS 实际 `SYSCLK`
2. 再做 CW 锁定误差复测
3. 再做 AM 解调页复测
4. 最后判断 `Rate(est)` 是否单独修算法
