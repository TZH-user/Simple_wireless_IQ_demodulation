# 方案改动记录

## 2026-05-21 扫频校准模式与任务模式方案

### 本次补充
- 非平台候选中心规则从“跳过边缘后的段内最低点”改为“候选段内两个最高峰之间的最低点”。
- 校准基线差分增加 `APP_SWEEP_CAL_BASELINE_MIN_VPP` 门限，低于门限的基线点不再扣减任务扫频 Vpp。
- 校准差分从 `max(raw_vpp - baseline_vpp, 0)` 调整为 `max(raw_vpp - baseline_vpp, 0) + baseline_avg_vpp`，避免矫正后波形被剪到过低。
- 候选增加低能量过滤门限：`APP_SWEEP_CANDIDATE_MIN_PEAK_VPP` 和 `APP_SWEEP_CANDIDATE_MIN_SCORE_VPP`。
- 无有效候选时最多重复粗扫 `APP_SWEEP_MAX_RETRY_COUNT` 次；仍失败则返回未锁定，任务层不进入分析。

### 当前采用方案
- 上电后默认停在 `待选择`，不自动进入扫频和分析。
- UI 提供 `校准` 和 `任务` 两个入口：
  - `校准` 用于发射机关闭时采集环境基线，结果只保存在 RAM。
  - `任务` 用于发射机开启后执行扫频锁定和调制类型判别。
- 任务扫频在校准基线有效、频率网格一致且基线幅度达到门限时，使用 `当前 Vpp - 基线 Vpp + 校准带内平均值` 做候选判定；无有效校准或基线低于门限时退回原始 Vpp。
- 本版只做扫频基线校准和 ADC 近轨检测，不做全频带 FFT 校准，不做掉电保存。

### 这样改的依据
- 当前空间环境存在固定干扰，单次任务扫频容易把环境强峰误判为发射信号。
- 校准基线能在不增加硬件复杂度的前提下降低固定干扰权重。
- 全频带 FFT 扫描耗时和状态复杂度更高，FM 最大频谱 bin 也不一定等于载频中心，因此先不作为 v1 主方案。

### 本轮保持不变
- 不修改 ADC/DMA 采样链路。
- 不修改 DDS 队列架构。
- 不做 Flash/掉电保存。
- 不把 UI 直接接入扫频内部状态，模式选择仍通过 `ModDetectTask` 统一调度。

### 经验说明
- 对嵌入式信号检测流程，校准基线和任务检测应先解耦为两个状态，再考虑 FFT 微调。
- 当外部环境干扰较多时，优先使用“同网格基线差分”降低固定干扰影响。
- 经验仅供参考，不视为绝对结论。

## 2026-05-17 相位闭环去死区方案

### 当前采用方案
- 相位环不再使用“误差进入死区就直接输出 0”的停控策略。
- 相位误差进入零附近后，仍然按现有 P 环、最小步进、最大步进、节拍和过零刹车逻辑运行。
- 保持区逻辑仍保留，但不再由死区条件触发。

### 这样改的依据
- 用户当前要验证的是零附近是否仍持续调节，而不是在死区内自动停控。
- 当前更合适的限速/稳态手段是最小步进、最大步进、节拍和过零刹车，而不是额外再加一层零输出死区。

### 本轮保持不变
- 不改 `KP/KI/KD` 参数。
- 不改 `APP_PHASE_HOLD_EXIT_MDEG` 保持区阈值。
- 不改过零刹车逻辑。

### 经验说明
- 当用户明确希望观察零附近连续控制效果时，零输出死区通常会掩盖真实控制特性，应优先去除。
- 经验仅供参考，不视为绝对结论。
## 2026-05-17 `phase_vofa_logging.patch` 合并方案

### 当前采用方案
- 不直接对当前工作树执行 `git apply`。
- 对路径已经漂移、接口已经分叉的补丁，采用“逐文件人工合并 + 编译验证”方案。
- 当前补丁的核心落点是：
  1. `iq_amp` 的 VOFA 调试输出改成相位/步进/电压相关多通道短报文；
  2. `phase_align` 状态结构补充相位变化率和 PID 诊断量；
  3. `phase_align` 控制逻辑补齐积分/限幅/速率观测字段，但保持现有主线接口不变。

### 这样改的依据
- 当前工程活动路径是 `Core/App/...`，补丁基线不是当前树；直接打补丁风险高。
- 当前主线已经围绕 `app_signal_ctrl.c -> carrier_sync -> phase_align` 重构过，强行生打容易把最近这轮扁平化主链打乱。
- 该补丁本质上是“日志增强 + 相位环状态增强”，适合按 hunk 有选择地并入。

### 本轮保持不变
- 不回退当前 locked 主链结构。
- 不恢复旧 `app_signal_detect` 兼容层。
- 不改变 sweep / IQ 预处理的判据。

### 经验说明
- 对 `.patch` 文件，先判断它是“同一基线的小改动”，还是“跨版本迁移补丁”。
- 前者优先 `git apply --check`；后者优先人工合并关键 hunk，不要为了省一步把当前主线打乱。
- 经验仅供参考，不视为绝对结论。
## 2026-05-15 锁定态“先判接管，再决定写 DAC”方案

### 当前采用方案
- 锁定态主链在当前帧按以下顺序运行：
  1. 先生成 IQ 结果
  2. 再刷新 carrier 观测
  3. 再由 phase 判断本帧是否接管
  4. 最后由 pipeline 决定本帧是否执行频率环写 DAC
- 这样接管逻辑在主链上是显式可见的，不再只是依靠 `carrier_sync.mode` 在下一轮进入频率环时再被动挡住。

### 这样改的依据
- 用户对原结构的主要不满不是算法错，而是阅读时必须跨 `pipeline / carrier / phase` 三处才能确认“接管后频率环什么时候停”。
- 当前方案把“谁写 DAC”的最终裁决点提升到 pipeline，更符合“主链固定角色顺序、避免逻辑分散”的整理目标。

### 本轮保持不变
- 不改 sweep 判据。
- 不改 IQ 预处理算法。
- 不改 `app_carrier_sync_calc_point_phase()` 数学方法。
- 不改相位接管参数和节拍。

### 经验说明
- 当同一条主链里存在两个可能的控制环时，应尽量把“谁拥有执行权”的判定放到调度层显式表达，而不是让阅读者依靠下游状态位跨一帧推断。
- 经验仅供参考，不视为绝对结论。

## 2026-05-15 锁定态主链收口方案

### 当前采用方案
- 锁定态主线按三个固定角色组织：
  1. IQ 结果生成
  2. Carrier 频率闭环与块内相位观测
  3. Phase 阶段按需接入
- 自动相位环未启用时，pipeline 中的 phase 阶段显式旁路，不再只是“函数被调用后立即返回”的隐式行为。

### 这样改的依据
- 当前锁定态阅读负担主要来自：IQ、carrier、phase 三个角色交织在同一函数里，而且 carrier 还套了一层只有单点调用的薄包装。
- 参考 `Simple_wireless_IQ_扫频状态机可读性与重构习惯_2026-05-14.md`，优先收主链、固定角色顺序，比先做局部微优化更有效。

### 本轮保持不变
- 不改 `residual_freq_millihz` 的估计方法。
- 不改 `app_carrier_sync_calc_point_phase()` 的相位观测算法。
- 不改相位接管控制律和节拍参数。

### 经验说明
- 对 block 驱动状态机，locked 主链也应尽量像 sweep 一样固定成少数几个稳定角色，避免“结果生成 / 控制 / 日志 / 相位接管”混在一个函数里。
- 经验仅供参考，不视为绝对结论。

## 2026-05-15 相位接管语义清理

### 当前采用方案
- 当前 `APP_CARRIER_SYNC_MODE_PHASE` 明确定义为“相位模块接管 DAC 的模式”。
- 不再在活动源码中保留“用户手动接管”“人工接管”这类旧表述，避免把自动相位接管与历史手动调压方案混为一谈。

### 这样改的依据
- 当前手动 DDS 相位微调和手动 OCXO VRFE 调压入口已经从活动主线移除。
- 若继续保留旧表述，会误导后续阅读者，以为当前 `phase` 模式仍依赖用户交互入口。

### 本轮保持不变
- 不改频率闭环控制律。
- 不改相位接管判定条件。
- 不改 `Pipeline -> Carrier -> Phase` 主线调度关系。

### 经验说明
- 代码语义和注释口径应尽量与当前活动主线一致，避免历史方案名称残留继续干扰阅读。
- 经验仅供参考，不视为绝对结论。

## 2026-05-14 相位接管后固定人工模式验证方案

### 当前采用方案
- 当前相位锁定验证版不再设置“频偏重新变大就退出”的规则。
- 当前一旦相位模块正式接管 DAC，就固定停留在 `manual` 模式。
- 当前只有两种情况会退出相位接管：
  - 失锁
  - 重扫

### 这样改的依据
- 用户当前目标是先验证“相位自动归零链路能否自己推着 DAC 走”，不是同时验证频率环和相位环之间的复杂切换逻辑。
- 如果在验证阶段继续保留“频偏冻结”和“自动退回频率环”，就会把问题混在一起，难以判断到底是相位方向、节拍，还是频率环抢回控制权导致现象异常。
- 因此这版采用更强的隔离策略：相位一旦接管，就只让相位模块控 DAC。

### 当前关键逻辑
- 进入条件：`locked_gate=1`、`phase_valid=1`、稳定时间达到门槛。
- 接管动作：先调用 `app_carrier_sync_manual_adjust_uv(0)`，仅切换到 `manual` 模式，不立即改电压。
- 推进方式：之后按相位误差正负号决定 DAC 增减方向，再按误差大小选择快/中/慢节拍。
- 接近 0 度时：进入死区保持，不继续调压。
- 接管后相位观测若暂时无效：仅保持当前 DAC，不退出接管。

### 经验说明
- 这版更适合先验证方向和节拍是否基本正确。
- 如果后续验证成功，再决定是否重新引入“退出相位接管并交回频率环”的保护逻辑。
- 经验仅供参考，不视为绝对结论。
## 2026-05-14 简化版慢速相位锁定方案

### 当前采用方案
- 不采用 PWM 占空比调压法。
- 采用更简单的“慢速加减速”方式做相位锁定：
  - 频率先锁住；
  - 相位误差大时稍快一点推 DAC；
  - 相位误差变小时自动减速；
  - 进入 0 度附近死区后停止调压。
- 当前相位锁定只依赖 `carrier_sync` 已经算好的相位误差和残余频偏，不再额外引入调制识别和复杂前置条件。

### 这样改的依据
- 用户已经通过手动 DAC 调压验证：当前链路下，VRFE 电压变化能够带动相位向目标方向移动。
- 因此自动相位锁定没必要再绕回 DDS，也没必要再做占空比脉冲调压，只要把人工“来回慢慢调”的行为变成受限速保护的自动步进即可。
- “大误差快一点、小误差慢一点”比固定速度更容易在接近 0 度时减速，减少来回穿过目标点的风险。这一点属于经验判断。

### 当前关键参数
- 进入相位锁定前的频偏门限：`50 mHz`
- 退出相位锁定并交回频率闭环的频偏门限：`120 mHz`
- 频率稳定等待时间：`1000 ms`
- 相位死区：`1.5°`
- DAC 固定步进：`1000 uV`
- 快速节拍：`800 ms`
- 中速节拍：`1200 ms`
- 慢速节拍：`1800 ms`

### 本轮保持不变
- 不改扫频判据。
- 不改 `AdcTask.c`。
- 不恢复屏幕 DDS 相位微调。
- 不恢复屏幕 OCXO 手动调压控件显示。
- 不修改 CubeMX 生成区。

### 调参建议
- 若相位还是过冲，优先继续放慢节拍，不要先加大步进。
- 若相位靠近 0 度太慢，再考虑把快速节拍从 `800 ms` 缩短，而不是直接增大 DAC 步进。
- 若方向反了，只改 `APP_PHASE_ALIGN_CONTROL_POLARITY`，不要同时动多个参数。
- 经验仅供参考，不视为绝对结论。
## 2026-05-14 锁频后手动 OCXO VRFE 调压方案

### 当前采用方案
- 暂不做“屏幕调频差”功能，也不再以 DDS 相位微调作为当前主方案。
- 当前改为：锁频后由屏幕按钮直接增减 `DAC1 -> OCXO VRFE` 控制电压。
- 用户一旦手动调压成功，DAC 控制权从自动频率闭环切到人工接管模式。
- 人工接管模式下保留相位和残余频偏观测，但自动频率闭环不再继续改 DAC。

### 这样改的依据
- 用户明确说明当前真正要调的是恒温晶振 `VRFE`，不是 DDS。
- 外部链路里，DAC 调压才是改变 OCXO 频率、进而改变相位旋转速度的主控制量；DDS 调相只会改参考相位，不符合当前目标。
- 如果用户按下加减后自动环仍继续写 DAC，会立即抵消人工调节，所以必须引入独立的 `manual` 模式。
- 当前 DAC 12-bit、3.3 V 参考下单 LSB 约 `806 uV`，因此人工步进采用 `1 mV` 比 `100 uV` 更容易稳定观察到真实码值变化。该点属于经验判断。

### 本轮保持不变
- 不改扫频判据。
- 不改 `AdcTask.c`。
- 不恢复相位自动闭环。
- 不实现 DDS 频差细调。
- 不修改 CubeMX 生成区。

### 后续建议
- 如果这版人工 DAC 接管能稳定把相位旋转方向和速度控制住，再考虑把人工步进从 `1 mV` 细化到更小级别，或者再包一层“人工频差偏置”。
- 若后续要恢复自动环，建议增加单独的“退出手动接管”动作，不要让自动环在人工模式下偷偷改 DAC。
- 经验仅供参考，不视为绝对结论。
## 2026-05-14 手动 DDS 相位微调平滑化方案

### 当前采用方案
- 保留“锁频后人工微调 `AD9959 CH0` 相位”的总体方案，不恢复自动相位闭环。
- 将屏幕步进从 `5°` 缩小到 `1°`，降低单次 `IO_Update()` 带来的相位台阶感。
- 为屏幕按钮增加长按连续重复事件，使用户能在输出过程中连续小步挪动解调参考相位。

### 这样改的依据
- 当前硬件链路是外部正交解调器，`AD9959 CH0` 只作为本振输入；只改该路 LO 相位即可带动外部解调后的 IQ 观测相位整体旋转。
- `AD9959` 当前命令路径是“改相位寄存器 + IO_Update()”，不会关通道或整片复位，但会产生离散相位台阶；因此减小步进比继续保留 `5°` 更适合细调。
- 长按连续微调只改变屏幕交互密度，不改变 DDS 控制分层和锁频门控。

### 本轮保持不变
- 不改扫频判据。
- 不改 `AdcTask.c`。
- 不改 `VRFE / OCXO` 频率闭环控制律。
- 不恢复相位自动闭环。
- 不修改 CubeMX 生成区。

### 经验说明
- 经验仅供参考，不视为绝对结论。
- 该方案能降低人工调相时的台阶感，但本质上仍是离散相位更新，不是连续模拟旋转。
## 2026-05-14 锁频后手动 DDS 相位微调方案

### 当前采用方案
- 频率闭环仍由 `VRFE / OCXO` 负责，不改原有 `residual_freq_millihz -> DAC` 主链。
- 相位微调不接入自动闭环，只提供“锁频后人工微调”入口。
- 手动微调对象固定为 `AD9959 CH0`。
- 手动微调步进固定为 `5°`，由屏幕上的 `-5 deg / +5 deg` 按钮触发。

### 职责边界
- `DDS control`：只负责新增“指定通道设置相位并立即 Apply”命令，不判断是否锁定。
- `Signal Pipeline`：负责锁频门控，只有 `stage == APP_SIGDET_STAGE_VPP_LOCKED && locked && carrier_present` 时才允许微调。
- `LVGL UI`：只负责屏幕按钮和当前 `CH0` 相位显示，不直接改硬件寄存器。

### 这样改的依据
- 用户当前需求是“通过屏幕手动微调”，不是恢复自动相位闭环。
- 把“是否已锁频”判断放在 `Pipeline`，比放进 DDS 层更符合当前总调度分层。
- 继续走 `AppDDS_DispatchCmd()` 队列路径，能避免 UI 线程跨层直接改 `AD9959` 状态。

### 本轮保持不变
- 不改扫频判据。
- 不改 `AdcTask.c`。
- 不改 VRFE 频率闭环控制律。
- 不恢复相位自动闭环。
- 不修改 CubeMX 生成区。

### 后续建议
- 若后续确认“只调 CH0”确实能让实际 IQ 观测相位按预期移动，则再决定是否把同样能力扩展到 `CH1` 或改为 I/Q 成对相位平移。
- 若后续要做自动相位归零，应在这套手动入口之外单独设计状态机和保护，不要直接把屏幕步进逻辑改造成自动闭环。
- 经验仅供参考，不视为绝对结论。
## 2026-05-13 SI5351 参考时钟恢复方案

### 当前采用方案
- 不在 `DDSTask` 里绕过 `ready` 检查，也不临时删除等待逻辑。
- 直接恢复独立 `SI5351` 任务，让它负责板级参考时钟的启动与运行期监控。
- 继续复用 `app_si5351_drv` 包装层，不回退到旧的裸写寄存器任务实现。

### 这样改的依据
- `DDSTask` 等待 `app_si5351_is_clock_ready()` 本身没有问题，问题在于当前没有活动任务去把 ready 置位。
- 继续维持“SI5351 任务产出参考时钟，DDS 任务消费 ready 状态”的结构，职责最清晰，也最符合后续维护。
- 这是最小改动：只补缺失启动链路，不改 DDS 默认输出、扫频和频率闭环。

### 本轮保持不变
- `DDSTask` 的等待逻辑不变。
- DDS 默认 40 MHz 双通道启动投递逻辑不变。
- Signal Pipeline、Sweep 和 Carrier Sync 不变。

### 经验建议
- 后续若要改 SI5351 默认板级输出计划，优先改独立任务里的计划表，不要把板级参考策略塞进 DDS 或 Signal 层。
- 经验仅供参考，不视为绝对结论。

## 2026-05-13 Signal Pipeline 总调度方案落地

### 当前采用方案
- `AdcTask` 只负责物理层数据分发，不再承担算法调度。
- `app_signal_pipeline_process_block()` 成为唯一算法总调度入口。
- `Sweep` 只负责扫频和扫频结果，不再承担锁定后业务决策。
- `IQ Track` 独立成子模块，专管锁定后残余频偏分析和验证快照。
- `Carrier Sync` 只负责频率闭环和相位观测；相位闭环仍不启用。

### 当前模块边界
- `Sweep` 输出：`stage / carrier_present / locked / estimated_hz`。
- `Pipeline` 决定：是否允许锁定后 IQ 跟踪、是否调用 Carrier、何时触发算法日志。
- `Carrier Sync` 只根据 `app_iq_preproc_result_t` 和原始 IQ block 更新 VRFE 频率闭环。
- `app_signal_detect.c/.h` 仅保留兼容壳，不再视为活动主线。

### 当前调度顺序
- `AdcTask` -> `app_signal_pipeline_process_block()`
- `Pipeline` -> `app_signal_sweep_process_block()`
- `Pipeline` 读取 Sweep 状态并构造 `locked_gate`
- 未锁定：`app_signal_iq_track_reset()` + `app_carrier_sync_on_unlock()`
- 已锁定：`app_signal_iq_track_process_locked_block()` -> `app_signal_iq_track_get_snapshot()` -> `app_carrier_sync_process_locked_block()` -> IQ/Carrier 日志

### 本轮保持不变
- Sweep 的 coarse/fine/locked 判据不变。
- VRFE 频率闭环步进、死区、极性和中心电压不变。
- 不恢复相位闭环。
- 不修改 CubeMX 生成区。

### 经验建议
- 后续若再接入调制识别或相位闭环，应继续挂到 `Pipeline`，不要塞回 `Sweep`。
- 若后续要彻底移除兼容壳，应先把所有外部引用从 `app_signal_detect_*` 切到 `app_signal_pipeline_*`。
- 经验仅供参考，不视为绝对结论。

## 2026-05-13 `app_signal_detect.c` 历史死代码清理方案

### 当前采用方案
- `app_signal_detect.c` 只保留当前活动主线，删除文件尾部不参与编译的 `#if 0` 历史旧实现。
- 不改扫频状态机逻辑，不改频率闭环逻辑，不改 `AdcTask` 分发主流程。

### 这样改的依据
- 该历史块完全位于条件编译关闭路径内，对当前固件行为为零影响。
- 它会显著干扰代码审查，使人误以为这些旧字段、旧函数仍然参与主线。
- 先清掉死代码，再讨论是否继续拆活动代码，判断会更准确。

### 本轮保持不变
- `app_signal_pipeline_process_block()` 的主线顺序不变。
- `app_signal_iq_preproc_process_block()` 与 `app_carrier_sync_update()` 的调用关系不变。
- 扫频、锁定门控、频率闭环判据不变。

### 经验建议
- 如果后续继续瘦身，优先看活动代码里的“日志/验证/调度胶水”，不要再把历史旧实现放回主文件。
- 经验仅供参考，不视为绝对结论。

## 2026-05-13 频率闭环主线简化方案

### 当前采用方案
- 当前主线明确收缩为：`扫频锁定 -> IQ 残余频偏分析 -> VRFE 频率闭环`。
- 相位闭环接管、目标 residual 轨迹、远区方向保持、相位 DPLL 等方案，本轮全部从活动主线中移除。
- 保留逐点相位圆均值计算，但它只作为观测量服务串口和调试，不再参与 DAC 控制决策。

### 这样改的依据
- 当前真正影响 VRFE 的代码路径一直只有频率闭环；相位闭环相关代码已经停止接管 DAC，只剩状态字段和日志残留。
- 继续保留这些残留会让 `app_carrier_sync` 的接口、状态结构体和日志口径明显偏复杂，但对现阶段可运行功能没有正收益。
- `AdcTask.c` 不是当前复杂度来源，因此不在主流程上继续拆；仅做局部性能友好的统计缓存优化。

### 本轮保留不变
- 频率闭环控制关系不变：
  - 仍依据 `residual_freq_millihz` 的正负号调节 VRFE。
  - 仍使用 `APP_CARRIER_SYNC_STEP_UV` 固定步进。
  - 仍使用 `APP_CARRIER_SYNC_CONTROL_POLARITY` 决定调节方向。
- 扫频状态机不变。
- `AdcTask` 的 DMA 领取、DCache 失效和 `app_signal_pipeline_process_block()` 分发结构不变。
- 不改 CubeMX 生成区。

### 日志口径
- `car`：只看频率闭环状态、电压、DAC code、DAC code 变化和更新/保持计数。
- `carp`：只看逐点相位观测是否有效、当前绝对相位、相位误差、有效点数和集中度。
- 不再保留“是否相位接管”“目标 residual”“远区方向保持”等当前无控制意义的日志字段。

### 经验建议
- 如果后续要重新上相位锁定，不要在这版上把旧字段再一点点加回来。
- 更稳妥的做法是先确认“要不要重新接管 DAC”，再单独补一个小状态机和一组最小必要变量。
- 经验仅供参考，不视为绝对结论。

## 2026-05-06

### 方案来源
- 调度书：`D:/CodexProject/2026年校电赛/多AI执行调度书_20260506.md`
- 决策书：`D:/CodexProject/2026年校电赛/决策书_参数分析审查_20260506.md`

### 本轮采用方案
- `sigdet`：只补“最终锁定结果”日志，不打开完整 VPP 全过程日志
- `ASK`：采用 `env + edge + freq` 最小改动判据
- `PSK`：采用简化联合判据，不做 FFT，不做最大似然，不做方案 C

### 本轮落地参数
- `APP_MODDET_AM_ENV_PM_THRESHOLD = 350`
- `APP_MODDET_ASK_MIN_EDGE_PM = 40`
- `APP_MODDET_ASK_MIN_FREQ_PM = 80`
- `APP_MODDET_PSK_JUMP_PM_THRESHOLD = 12`
- `APP_MODDET_PSK_MIN_POS_PERCENT = 80`
- `APP_MODDET_PSK_MAX_NEG_PERCENT = 20`
- `APP_MODDET_PSK_MAX_ENV_PM = 200`

### 本轮分类顺序
- `NO_CARRIER`
- `ASK`
- `AM`
- `PSK`
- `FSK`
- `FM`
- `CW`

### 暂未执行
- FFT 辅助判别
- PSK 最大似然/星座匹配
- ASK 方案 B
- PSK 方案 C

### 备注
- 经验仅供参考，不视为绝对结论。
- 本轮仅完成代码侧最小改动，是否通过仍需后续测试数据验证。

### 2026-05-06 第二轮版本一致性确认
- 方案来源：`D:/CodexProject/2026年校电赛/AI分发_方案决策_20260506/01_方案决策AI_决策输出_20260506.md`
- 本轮未改动：`ASK/PSK` 判据、阈值、DDS/ADC 主链路
- 本轮只落地：
  - 添加唯一 `fw_id` 启动日志
  - 复核 `sigdet_final` 输出链路
- 当前固件标识：`fw_id,moddet_review2_20260506,build=20260506_0308,ask=edge_freq,psk=pos_only`
- 已验证结论：
  - `fw_id` 不依赖锁定状态，任务启动后即可发送
  - `sigdet_final` 位于 `app_signal_detect_finish_locked()`，输出链路为 `print_queue_send_log()`
  - `sigdet_final` 不受 `APP_SIGDET_VPP_UART_TRACE_ENABLE` 宏控制
- 经验建议：
  - 版本一致性确认阶段优先使用显式时间戳 `build=YYYYMMDD_HHMM`，比 `__DATE__` / `__TIME__` 更适合串口检索
  - 经验仅供参考，不视为绝对结论

### 2026-05-06 编码规则补充执行
- 本次不改业务方案，只按总控编码规则修正文档编码与损坏内容
- 已验证结论：中文 Markdown 协作文档应统一为 `UTF-8 with BOM`
- 经验建议：读取中文 Markdown 必须显式 `-Encoding UTF8`；经验仅供参考，不视为绝对结论

### 2026-05-06 调度门禁收紧
- 本次不改算法方案，只收紧执行顺序与放行条件
- 已验证结论：
  - 只有“实时串口中的当前烧录版本 `fw_id`”与“采集日志中的同一版本 `fw_id`”都成立后，最小验证集才可视为有效输入
  - 若实时串口有当前烧录版本 `fw_id` 但采集日志没有命中同一版本 `fw_id`，则优先问题在采集链路，不在算法判据
- 经验建议：
  - 后续涉及多 AI 串行协作时，把“实时观测门槛”和“采集文件门槛”拆成两级，能更快定位责任边界
  - 经验仅供参考，不视为绝对结论


### 2026-05-06 `fw_id` 启动链路加固
- 本次不改算法方案，只增强启动日志可见性
- 已验证结论：
  - 对“上电前开始抓取”的场景，`fw_id` 由 `PrintfTask` 直接发送比在 `AdcTask` 中先入队更可靠
  - 在 `g_uart_mode == UART_MODE_LOG` 下，启动后 500ms 周期重复输出几次，能更稳地覆盖抓取启动时机误差
- 经验建议：
  - 对版本一致性门禁类日志，优先放在最靠近最终串口发送的一侧
  - 经验仅供参考，不视为绝对结论

### 2026-05-06 `fw_id` 启动链路加固
- 本次不改算法方案，只增强启动日志可见性
- 已验证结论：
  - 对“上电前开始抓取”的场景，`fw_id` 由 `PrintfTask` 直接发送比在 `AdcTask` 中先入队更可靠
  - 在 `g_uart_mode == UART_MODE_LOG` 下，启动后 500ms 周期重复输出几次，能更稳地覆盖抓取启动时机误差
- 经验建议：
  - 对版本一致性门禁类日志，优先放在最靠近最终串口发送的一侧
  - 经验仅供参考，不视为绝对结论

### 2026-05-06 调参结果审查与 UI 参数显示方案
- 依据：调参工程师 tune4 结果，30/40/50 MHz x CW/AM/FM/ASK/FSK/PSK 多数判决 `18/18` 通过
- 已验证分类问题：`ASK_30M` 存在 AM/ASK 跳变但多数正确，`FSK_30M` 存在 FM/FSK 跳变但多数正确；未发现完全错误用例
- 本次采用方案：先做屏幕参数估计显示与稳定模式显示，不立即把 FFT 接为主分类器
- 当前参数估计方法：包络过零/翻转估计调制率，包络幅度平方开根估计 AM/ASK 深度，`dphi` 小角度近似估计 FM/FSK 平均频偏
- 后续建议：优先做 ASK/FSK 稳定性增强和 Goertzel/小窗口频点检测；FFT 作为参数估计辅助，不建议立即重构主分类器
- 经验建议：参数估计值先按“估计值”显示，后续用信号源标称值做校准；经验仅供参考，不视为绝对结论

- 校正：参数估计默认采样率按 TIM6 时钟 240MHz / Period 75 = 3.2MS/s，已将 `APP_MODDET_DEFAULT_SAMPLE_RATE_HZ` 设为 `3200000UL`。

## 2026-05-06 工程健康度与调试探针审查

- 新增审查输出：`D:\CodexProject\2026年校电赛\AI分发_方案决策_20260506\17_方案决策AI_工程健康度与调试探针审查_20260506.md`
- 审查对象：`D:\VSCodeCMAKEProject\Simple_wireless_IQ_demodulation` 当前源码与用户截图中的 `AdcTask.c` 14-bit ADC 阈值修正。
- 结论：图片中的 ADC 阈值改动通过；当前工程为测试/调参固件状态，不是最终提交态。
- 统计：7 处活跃串口输出探针、4 组默认关闭探针、1 组 ADC 幅度测试临时代码、14 个源码目录备份文件、9 行乱码注释。
- 风险：`adc_raw_to_v_1e4()` 仍按 16-bit 换算但当前路径关闭；`adc_range` 与 `ui/mod` 调试日志仍开启；本地环境缺少 `cmake/ninja`，本轮未能重新构建。

## 2026-05-07 基带频谱显示接入

- 方案来源：`D:/CodexProject/2026年校电赛/AI分发_方案决策_20260506/24_发给改代码AI_基带频谱显示接入任务书_20260507.md`
- 前置放行：`23_方案决策AI_载波锁定误差审查与基带频谱放行_20260507.md`

### 本轮采用方案
- 基带频谱仅作为观测/UI 功能接入，不进入主分类判据
- 只在 `APP_SIGDET_STAGE_VPP_LOCKED && locked && carrier_present` 后提交数据
- 采用“`AdcTask` 提交快照 + 独立低优先级频谱任务计算 + UI 只读状态”的解耦结构
- 采用 `256` 点、约 `200kHz` 等效采样率的小窗口复数 DFT，优先保证刷新速度
- UI 刷新周期设为 `80ms`，目标每秒 `12~13` 次刷新

### 已验证结论

## 2026-05-07 频谱接入回退

- 用户决策：当前先回退到“未接入基带频谱”状态。
- 本轮采用方案：
  - 撤回 `app_baseband_spectrum` 与 `BasebandSpectrumTask`
  - 撤回 UI 底部频谱柱图
  - 恢复“FFT 预留区”占位显示
  - 维持 `carrier_lockerr_20260507` 固件身份
- 保留项：
  - 条件半步修正后的载波锁定逻辑
  - 当前 `app_mod_detect` 参数识别与稳定模式显示
  - ADC 幅度监测与现有调试日志
- 暂缓项：
  - 任何基于 FFT 的调制主判据接入
  - 频谱日志与频谱 UI 恢复

## 2026-05-07 AdcTask BusFault 修复

- 现象：锁定后进入调制识别路径，在 `print_queue_send_log()` 调用 `osMessageQueuePut()` 时触发 BusFault
- 本轮采用方案：
  - 优先按“任务栈过小 + 日志队列路径过脆”处理
  - 扩大 `AdcTask / PrintfTask` 栈
  - 将日志队列发送改为非阻塞丢弃
  - 保留现有调制识别逻辑，不改判据
- 当前判断：
  - 这是 RTOS 运行期稳健性问题
  - 不是载波锁定修正逻辑问题
  - 不是频谱回退本身引入的新算法问题
- 在不改 ADC/DDS 底层、不改调制分类阈值的前提下，可以独立接入锁定后基带频谱摘要显示
- 频谱状态在未锁定时主动清空为 `valid=0`，可避免 UI 显示旧值
- 新增 `bbsp` 低频日志不会替换或移除原有 `sigdet` / `mod` 日志链路

### 经验建议
- 对高频 ADC 路径上的观测功能，优先采用“快照提交 + 低优先级后台任务”的旁路结构，实时性风险更低
- 经验仅供参考，不视为绝对结论
## 2026-05-07 复数基带频谱柱状图方案写入

- 方案来源：`58_bbsp_complex_spectrum_pending_20260507_110651`
- 本轮采用方案：
  - 复数基带频谱显示
  - `-100kHz ~ +100kHz`
  - `41` 柱
  - `5kHz` 步进
  - 优先按 Goertzel/定点频点能量思路实现
- 保持不变：
  - ADC/DDS 底层
  - 载波锁定逻辑
  - 调制识别判据与顺序
  - 独立低优先级频谱任务结构

### 已验证结论
- 在当前独立频谱任务框架内，可以进一步升级为固定频点复数基带柱状频谱，而不需要把频谱计算压回 `AdcTask` 高频路径
- `41` 柱与 `5kHz` 步进可以正好对齐 `-100kHz ~ +100kHz`，中间柱即 `0Hz`
- UI 侧只读状态并绘制柱图，仍符合既定分层

### 经验建议
- 对锁定后基带观测任务，若目标频带固定，优先固定频点能量法，再考虑全频 FFT
- 经验仅供参考，不视为绝对结论

## 2026-05-07 实时解调界面方案（进行中）

- 本轮采用方案：
  - 识别页与解调页分离，使用双页面容器切换，不重建整屏对象
  - `DemodTask` 默认挂起，仅在进入解调页时恢复
  - `AdcTask` 只在载波锁定且 `app_demod.active=1` 时并行投递解调快照
  - 解调页首版只正式支持 `AM`，其他模式先占位显示 unsupported
  - 解调主显示优先采用 `lv_chart` 波形图，不先接音频链

### 已验证结论
- `DemodTask` 快照区移到 AXI SRAM 后，DTCMRAM 占用恢复到安全范围
- `DemodTask = osPriorityNormal, stack = 1024 * 4` 可满足当前首版结构

### 经验建议
- 后续若接入 `FM/FSK/PSK`，继续按 `app_demod_xxx.c` 分文件扩展，不回收为单文件
- 经验仅供参考，不视为绝对结论

- [2026-05-07] 解调页退出交互保持最小改动：优先修正 LVGL 事件触发条件与控件层级，不改任务链与状态机。

- [2026-05-07] 解调接入阶段的任务优先级策略调整：保持 AdcTask > DemodTask > ModDetectTask，同时确保 TouchTask 不低于 DisplayTask，以优先保障人机交互。

- [2026-05-07] 新增恒温晶振接入后的独立复测方案文档，先复测 DDS 时钟、锁定误差、AM 解调页与 Rate(est)，再决定是否修改载波锁定或参数识别代码。

## 2026-05-07 SI5351 外部参考最小改动方案落地

- 方案前提：
  - `OCXO 10MHz -> SI5351 CLKIN -> SI5351 CLK0 25MHz -> AD9959 REF_CLK`
  - `AD9959 CLK_MODE_SEL` 已固定为外部参考输入模式
  - 独立 `I2C1(PB6/PB7)` 已由 `CubeMX` 生成

### 本轮采用方案
- `SI5351` 只实现单一固定链路，不做通用频率框架
- `PLLA` 参考源固定选择 `CLKIN`
- `PLLA` 固定配置为 `800MHz`
- `CLK0` 固定输出 `25MHz` 给 `AD9959`
- `DDSTask` 在 `SI5351` 参考未就绪前不执行 `AppDDS_Init()`
- 运行期若检测到 `LOS_CLKIN` 或 `LOL_A`，关闭 `CLK0` 并告警，不切回本地晶振

### 已验证结论
- 在保持 `AD9959` 参考频率仍为 `25MHz` 的前提下，可以只改 `SI5351` 侧和任务启动时序，而不改 `ad9959.c` 中的 `FR1` 与 `FTW` 计算
- `CubeMX` 先生成 `I2C1` 基底后，再在独立用户文件里接 `SI5351` 逻辑，可以规避后续再生成时覆盖核心实现
- `DDSTask` 只增加“等待参考时钟就绪”这一道门禁，就能保证 `AD9959` 初始化不会早于外部 `25MHz` 稳定

### 经验建议
- `SI5351` 板级地址若不完全确定，优先在软件里兼容探测 `0x60/0x61`，比写死地址更稳
- 后续若要加入掉电恢复或热插拔恢复，优先继续在 `SI5351` 任务层扩展，不要先把逻辑塞回 `AD9959` 驱动
- 经验仅供参考，不视为绝对结论

## 2026-05-08 SI5351 双路参考时钟方案

- 方案前提：
  - `OCXO 10MHz -> SI5351 CLKIN`
  - `SI5351 CLK0/CLK1 -> 25MHz`
  - `AD9959` 仍按外部 `25MHz` 参考工作

### 本轮采用方案
- `CLK1` 与 `CLK0` 共用 `PLLA 800MHz`
- `CLK1` 也配置为整数分频 `25MHz`
- `CLK0/CLK1` 均采用 `8mA` 驱动
- 保持 `DDSTask` 只等待同一个 `SI5351` 就绪标志，不新增第二套时序逻辑

### 已验证结论
- 在当前 `PLLA=800MHz` 固定框架内，额外再开一路 `CLK1=25MHz` 不需要改 `AD9959` 初始化模型
- 对送 `AD9959` 的参考时钟用途，先用 `8mA` 驱动更稳妥，便于优先保证边沿速度和时钟裕量

### 经验建议
- 若后续示波器在 `CLK0/CLK1` 近端看到明显过冲、振铃或边沿过陡，可优先把驱动从 `8mA` 降到 `6mA` 再复测
- 若 `CLK1` 实际不接负载，保留开启不会影响现有 `CLK0=25MHz` 主链路，但仍建议按真实用途复测串扰和波形质量
- 经验仅供参考，不视为绝对结论

## 2026-05-08 零中频残余相位预补偿方案

- 方案背景：
  - 本振与来波不能做到绝对同频
  - 锁定后仍存在 `mHz` 级残余差频
  - 用户观察到 `I/Q` 两路主幅度会以约 `5~6s` 周期缓慢交换

### 本轮采用方案
- 保留零中频，不立即切到 `low-IF`
- 先落“第一阶段数字补偿”：
  - 以 block 均值矢量为相位观测量
  - 跨 block 累计残余相位
  - 在调制识别链与解调链入口处先做数字反旋转
- 插入位置选择在 `ModDetectTask` / `DemodTask`
  - 保持 `AdcTask` 高频路径不增重
  - 保持 `app_mod_detect.c` / `app_demod_am.c` 现有判据主体不先重写

### 已验证结论
- 对当前“慢速相位旋转、而非快速失锁”的现象，先用共享预处理在低优先级任务层做跨 block 相位预旋转，风险最小
- 该方案主要目标是稳定 `I/Q` 方向与后级参数识别入口，不等价于完整的复数 DC/载波泄漏消除

### 后续保留项
- 若实测后仍存在明显“模长呼吸”或接近归零，再进入第二阶段：
  - 追加复数 DC 矢量/圆心估计
  - 必要时再评估是否切到 `low-IF`

### 经验建议
- 对秒级缓慢残余差频，优先用跨 block 相位跟踪，不要用逐采样 `atan2()` 直接硬累加
- 若后续需要定量观察补偿效果，建议再补一条低频日志，输出残余频偏估计值与 block 均值矢量幅度
- 经验仅供参考，不视为绝对结论

## 2026-05-08 解调任务临时脱开主线方案

### 本轮采用方案
- 保留主页面 `Enter Demod` 按钮与解调页结构
- 保留 `DemodTask` 初始化与启停逻辑
- 从 `AdcTask` 中移除实时解调投递，使解调链不再参与当前主线采样处理
- 进入解调页后仅保持任务空转待机，直到用户按 `Back` 退出

### 已验证结论
- 该改动不影响载波锁定与调制识别主线
- 该改动可保留后续重新挂回解调链的页面与任务骨架

### 经验建议
- 若后续重新接回解调链，优先在 `AdcTask.c` 单点恢复 `demod_task_submit_block()`，不要先改 UI/任务框架
- 经验仅供参考，不视为绝对结论

## 2026-05-08 解调链收缩为页面空转壳方案

### 本轮采用方案
- 保留主页面 `Enter Demod` 与解调页 `Back`
- 保留 `DemodTask` 任务存在，但其职责仅为页面进入后的空转挂起壳
- 删除实时解调算法文件与处理入口，不再保留“待重新接回即可直接解调”的中间态
- 主线继续只保留载波锁定与调制识别

### 已验证结论
- 在当前阶段，仅保留空转壳可明显减少 RAM/FLASH 占用，也避免误用旧解调链
- 若后续需要恢复实时解调，应优先参考 `*.bak_20260508_iqpreproc` 与 `*.bak_20260508_demod_detach` 备份，而不是直接从当前代码反推

### 经验建议
- 后续若重建解调链，先明确是否还需要 IQ 预处理；不要直接把旧 `DemodTask.c` 全量拷回
- 经验仅供参考，不视为绝对结论

## 2026-05-08 参数识别/调制判别链收缩为空转壳

### 本轮采用方案
- 当前主线只保留载波锁定与检测页 UI
- `ModDetectTask` 保留任务实体，但不再消费 IQ、不再做参数识别、不再输出调制判据
- `app_mod_detect/app_iq_preproc/CMSIS-DSP` 全部移出当前构建，避免继续干扰主线收敛

### 已验证结论
- 对当前“先稳住主线、后重建算法”的阶段目标，直接把调制识别链从运行链和构建链同时摘掉，比保留半接线状态更稳妥
- 保留空转壳可以避免后续再动 `CubeMX` 任务配置，也保留未来重新接回的任务挂点

### 重点备份
- `Core/App/Tasks/ModDetectTask.c.bak_20260508_iqpreproc`
- `Core/App/Tasks/AdcTask.c.bak_20260508_demod_detach`
- `Core/App/LVGL/app_lvgl_ui.c.bak_20260508_demod_detach`
- `CMakeLists.txt.bak_20260508_iqpreproc`

### 经验建议
- 后续重建参数识别算法时，应直接新建一条清晰的数据链，不要在当前空转壳版本上局部补丁式恢复旧逻辑
- 经验仅供参考，不视为绝对结论

## 2026-05-09 IQ 预处理分析门控修正

### 本轮采用方案
- `app_iq_preproc` 只在 `sigdet` 已锁定状态下分析当前 ADC block
- coarse/fine scan、未锁定、失锁重扫阶段一律不参与残余频偏估计
- 每次退出锁定态后立即清空 `iq_preproc` 历史状态；重新进入锁定态后从新窗口重新积累

### 已验证结论
- 若在锁定前就持续喂入扫频阶段 IQ，`residual_freq_millihz` 会混入扫频过程本身的相位变化，不能代表锁定后的残留载波
- 对“几百 mHz 级别慢速残余频偏”这类目标，先保证分析窗口只覆盖稳定锁定段，比先改估计算法更重要

### 经验建议
- 后续若继续保留 block 级频偏分析日志，优先同时记录“是否锁定”和“分析块计数清零时刻”，便于区分新一轮锁定窗口
- 经验仅供参考，不视为绝对结论

## 2026-05-11 扫频独立与 OCXO VRFE 慢速闭环方案落地

### 本轮采用方案
- 将扫频状态机独立到 `Core/App/Signal/Sweep/app_signal_sweep.c/.h`
- 将 OCXO VRFE 控制独立到 `Core/App/Signal/Carrier/app_carrier_sync.c/.h`
- `AdcTask.c` 只保留 ADC DMA block 领取、DCache 失效、VOFA 可选输出、健康/幅度日志与同步分发入口
- `app_signal_detect.c` 作为 Signal 门面层，统一协调扫频、锁定门控、IQ 预处理和 VRFE 慢速闭环
- VRFE 默认中心电压为 `1550mV`，默认限幅 `1400mV ~ 1700mV`，锁定后按 `residual_freq_millihz` 每秒最多调整 `1mV`

### 已验证结论
- 在不新增实时 `SignalTask` 的前提下，可以把扫频与闭环逻辑从 `AdcTask.c` 移出，仍保持 2ms ADC 双缓冲同步处理结构。
- 当前实现未修改 CubeMX 生成的 `dac.c`、`tim.c`、`.ioc`，后续 CubeMX 再生成不会覆盖新增的 `Signal/Sweep` 与 `Signal/Carrier` 用户文件。
- `Debug` 构建通过：
  - `DTCMRAM: 76040 B / 128 KB (58.01%)`
  - `FLASH: 617572 B / 2 MB (29.45%)`

### 经验建议
- 第一轮上板时先观察 `DAC1_OUT1(PA4)` 是否稳定在约 `1.55V`，再接入信号源观察 residual mHz 收敛方向。
- 若残余频偏越调越大，优先只改 `APP_CARRIER_SYNC_CONTROL_POLARITY`，不要改扫频与 ADC 任务结构。
- 若后续要增强 UI/日志显示 VRFE 状态，优先读取 `app_carrier_sync_get_status()`，不要让 UI 直接访问 DAC HAL。
- 经验仅供参考，不视为绝对结论

## 2026-05-11 新拆分代码注释补充方案

### 本轮采用方案
- 参考 `D:\Downloads\comment_cleaned_sources_bundle` 中已清理的注释风格。
- 优先补充当前活动路径中的必要注释，而不是大范围改写旧历史代码。
- 注释重点放在：
  - 扫频状态机结构体和阶段切换
  - Signal 门面层的 block 流转和锁定门控
  - OCXO VRFE 闭环的中心电压、限幅、死区、极性和 DAC 写入
  - 公开接口的调用边界

### 已验证结论
- 注释补充不改变运行逻辑，`Debug` 构建通过。
- 当前活动文件未发现常见 mojibake 注释残留。
- 本次未修改 CubeMX 生成配置文件，后续 CubeMX 再生成不会覆盖新增用户模块注释。

### 经验建议
- 后续新增 `.c/.h` 时，先写清模块职责、调用边界和状态字段含义，再写实现细节注释。
- 对已迁移但暂留的 `#if 0` 历史参考块，等新模块上板验证稳定后，再单独申请授权清理，避免当前阶段混入大范围删除。
- 经验仅供参考，不视为绝对结论

## 2026-05-12 OCXO VRFE 闭环控制单位改为 uV

### 本轮采用方案
- 将 `app_carrier_sync` 内部控制量由 `mV` 改为 `uV` 定点整数，避免使用 `0.1mV` 这类 C 宏小数时被整数路径截断或引入浮点计算。
- 保持 DAC 输出仍通过 `HAL_DAC_SetValue()` 写入 12bit code，只在用户独立文件 `Core/App/Signal/Carrier/app_carrier_sync.c/.h` 内完成 `uV -> DAC code` 换算。
- 当前中心点按实测较优值设置为 `1590000uV`，默认限幅为 `1400000uV ~ 1700000uV`，单步调节改为 `APP_CARRIER_SYNC_STEP_UV`。

### 已验证结论
- `APP_CARRIER_SYNC_STEP_UV 100` 等价于原计划的 `0.1mV`，但全程走整数定点路径，不需要小数宏。
- 当前闭环单次合格更新最多改变 `100uV`；若 `APP_CARRIER_SYNC_UPDATE_PERIOD_MS` 仍为 `0U`，实际更新节奏由锁定后的 IQ block 调用频率决定。
- `Debug` 构建通过：
  - `DTCMRAM: 76040 B / 128 KB (58.01%)`
  - `FLASH: 617620 B / 2 MB (29.45%)`

### 经验建议
- 需要更慢时优先增大 `APP_CARRIER_SYNC_UPDATE_PERIOD_MS`，例如 `100U`、`200U`、`1000U`；需要更细时减小 `APP_CARRIER_SYNC_STEP_UV`。
- 需要更快但仍保持稳定时，优先小幅增大 `APP_CARRIER_SYNC_STEP_UV`，例如 `200U` 或 `500U`，并同时观察 residual mHz 是否出现来回越调。
- 经验仅供参考，不视为绝对结论

## 2026-05-12 锁定后自动进入 x 轴绝对相位锁定

### 本轮采用方案
- 保留现有扫频/Vpp 锁定判据，不把 `iq_rot` 日志里的 `rd/cd` 直接并入锁定条件。
- `rd`、`cd` 继续作为锁定后 IQ 旋转效果验证量：`rd` 观察原始头尾相位差，`cd` 观察补偿后的头尾相位差。
- 锁定门控满足 `APP_SIGDET_STAGE_VPP_LOCKED && locked && carrier_present` 后，Signal 层自动从 IQ 分析模式切到 IQ 旋转输出模式。
- IQ 旋转输出采用绝对相位锁定：用当前 block 的平均 IQ 相位作为锚点，把输出平均相位压到 `+I` 轴，也就是 x 轴。

### 已验证结论
- 当前实现不改变扫频阶段行为；扫频、未锁定、失锁重扫阶段仍不会进入 IQ 旋转输出模式。
- `Debug` 构建通过且无新增 warning/error。
- 启用旋转输出缓冲后 DTCMRAM 占用升至：
  - `DTCMRAM: 92424 B / 128 KB (70.51%)`
  - `FLASH: 617924 B / 2 MB (29.46%)`

### 经验建议
- 上板验证时重点看锁定后的 `iq_rot`：若 `cd` 明显小于 `rd`，说明频偏补偿有效；若下游查看旋转后的 IQ，平均 Q 应接近 ADC 中点，平均 I 作为主轴。
- 若后续发现每个 block 独立压到 x 轴导致调制相位信息被过度抹平，可将 `APP_IQ_PREPROC_ABS_PHASE_LOCK_X` 改为 `0U`，退回连续相位模式。
- 经验仅供参考，不视为绝对结论

### 2026-05-12 追加明确约束
- 相位模式的目标明确为锁定到 x 轴，也就是旋转输出后的平均向量满足 `tan = Q / I = 0`。
- 实现上不再只用 block 中心相位近似，而是先按当前残余频偏对整个 block 做虚拟旋转求和，再把这个合向量旋到 `+I` 轴。
- 因此 `cd` 仍是头尾相位差验证量；x 轴锁定目标对应的是输出 block 平均 Q 分量趋近 0。
- `Debug` 构建通过：
  - `DTCMRAM: 92424 B / 128 KB (70.51%)`
  - `FLASH: 618204 B / 2 MB (29.48%)`

### 2026-05-12 再次纠正
- 相位模式最终定义改为：由当前 I/Q 两路去中心幅度直接计算 `phase = atan2(Q, I)`，然后把该相位旋到 0。
- `rd/cd` 仍只作为验证日志，不参与相位锁定角计算。
- 残余频偏估计继续供 OCXO VRFE 慢速闭环使用，但不再参与 x 轴绝对相位锁定的旋转角。
- `Debug` 构建通过：
  - `DTCMRAM: 92424 B / 128 KB (70.51%)`
  - `FLASH: 617828 B / 2 MB (29.46%)`

### 2026-05-12 示波器锁定点复位随机修正
- 已确认：如果示波器看的是原始模拟 I/Q 或 DDS/混频输出，软件内部旋转无法改变示波器点位。
- 因此增加锁定后一次性 DDS 相位补偿：锁定后等待 8 个 block，用 `atan2(Q, I)` 得到相位误差，再写入 `APP_SIGDET_DDS_CHANNEL` 的 DDS 相位寄存器并 Apply。
- 该补偿只在每轮锁定后执行一次；失锁或重扫后复位状态，下一轮重新补偿。
- 若上板发现点位往反方向跑，只改 `APP_SIGNAL_PHASE_ALIGN_POLARITY`，不要改扫频和 VRFE 闭环参数。
- `Debug` 构建通过：
  - `DTCMRAM: 92432 B / 128 KB (70.52%)`
  - `FLASH: 618420 B / 2 MB (29.49%)`

### 2026-05-12 DDS 相位补偿撤销
- 当前需求重新明确为：只让 IQ 两路在软件矢量图上锁定到 x 轴，不操作 DDS 相位寄存器。
- 因此撤销上一版锁定后一次性 DDS 相位补偿方案，保留 `atan2(Q, I)` 计算和 IQ 软件旋转。
- 后续审阅时以该条为准：相位模式只处理软件 IQ 矢量，不改硬件 DDS 相位；`x_axis_phase_rad` 仅用于观察和软件旋转。

## 2026-05-12 VRFE 闭环切换为相位闭环

### 本轮采用方案
- 将 OCXO VRFE 控制量从 `residual_freq_millihz` 改为 `x_axis_phase_rad`。
- `x_axis_phase_rad` 来自 `atan2(Q, I)`，目标是 0；闭环通过微调 VRFE 让相位误差慢慢回到 x 轴。
- `residual_freq_millihz` 仍保留为观测量，用于判断是否存在持续频偏，但不再直接决定 VRFE 步进方向。
- 相位环默认参数：
  - `APP_CARRIER_SYNC_PHASE_UPDATE_PERIOD_MS = 20U`
  - `APP_CARRIER_SYNC_PHASE_DEADBAND_URAD = 5000`
  - `APP_CARRIER_SYNC_STEP_UV = 50`
  - `APP_CARRIER_SYNC_PHASE_POLARITY = 1`

### 已验证结论
- `Debug` 构建通过，无新增 warning/error。
- `iq_pre` 日志新增 `xp_urad` 和 `xv`，可直接观察相位误差和有效标志。
- 本次仍未修改 CubeMX 生成文件，也未操作 DDS 相位寄存器。

### 经验建议
- 上板第一轮只观察 `xp_urad` 是否向 0 收敛，不要同时改中心电压、步进和极性。
- 若 `xp_urad` 越调越远，优先只改 `APP_CARRIER_SYNC_PHASE_POLARITY` 为 `-1`。
- 若 `xp_urad` 来回越过 0 且震荡较大，优先减小 `APP_CARRIER_SYNC_STEP_UV` 或增大 `APP_CARRIER_SYNC_PHASE_UPDATE_PERIOD_MS`。
- 经验仅供参考，不视为绝对结论。

## 2026-05-12 回退：取消相位锁定方案

### 当前采用方案
- 按用户要求回退到不加相位锁定的版本。
- 扫频/Vpp 锁定状态机不变，仍由 Signal 层在锁定后打开 IQ 频偏分析和 VRFE 闭环门控。
- IQ 预处理只输出平均 IQ、残余频偏和相位累加诊断，不再输出 `atan2(Q, I)` 的 x 轴相位误差。
- OCXO VRFE 闭环控制量恢复为 `residual_freq_millihz`，目标是减小残余频偏，不再直接把绝对相位推到 x 轴。
- 运行时 IQ 旋转输出路径默认关闭，`AdcTask` 仍只通过 Signal pipeline 分发 DMA block。

### 审阅要点
- 看 `Core/App/Signal/app_signal_detect.c`：
  - `ADC_IQ_PREPROC_ROTATE_RUNTIME_ENABLE = 0U`，锁定后调用 `app_iq_preproc_analyze_block_u16()`。
- 看 `Core/App/Signal/Carrier/app_carrier_sync.c`：
  - `app_carrier_sync_update()` 读取 `iq_result->residual_freq_millihz` 并按 `APP_CARRIER_SYNC_STEP_UV` 调整 VRFE。
- 看 `Core/App/LOG/app_adc_log.c`：
  - `iq_pre` 日志不再包含 `xp_urad/xv`。

### 后续边界
- 当前版本不保证每次复位后 IQ 绝对相位落在同一个点。
- 若后续还需要“锁到 x 轴”，应重新单独设计相位闭环的采样相位定义、调制适配、环路带宽和失锁保护，不再把它混入本次频率闭环版本。
- 本次仍未修改 CubeMX 生成文件，也未操作 DDS 相位寄存器。

## 2026-05-12 方案更新：频率稳定后相位归零闭环

### 当前采用方案
- 总流程改为：`扫频锁定 -> 频率闭环 -> 调制初识别 -> 相位归零闭环 -> 调制复核/解调`。
- 相位目标统一为 `atan2(Q, I) = 0`，也就是 IQ 重心落在 `+I` 轴。
- 相位环在频率闭环得到的当前 VRFE 电压基础上接管 DAC 微调；接管后频率误差只做保护和退出条件。
- 所有调制类型都进入相位模块，但重心不可靠、调制识别置信度不足或频偏重新变大时冻结，不强行调节 VRFE。

### 时序策略
- ADC 仍按原 2 ms block 节拍进入 Signal pipeline。
- 调制识别通过 `ModDetectTask` 异步运行，Signal 层默认每 `100 ms` 提交一次锁定后的 IQ block。
- 相位稳定门槛采用 3 秒窗口 RMS：`phase_rms <= 30°`。
- 相位环默认更新周期为 `20 ms`，每次按 `APP_CARRIER_SYNC_PHASE_STEP_UV` 小步进调 DAC。

### 调制类型处理
- CW：直接用去中心 IQ 重心相位归零。
- AM/ASK：低幅度段不参与重心统计，只让有载波幅度的点在 `+I` 半轴摆动。
- FM/FSK/PSK：按统计重心归零，但提高重心可靠度门限；不可靠时冻结，避免把调制内容当作相位误差。

### 工程边界
- 不修改 CubeMX 生成配置。
- 不操作 DDS 相位寄存器。
- 调制识别 FFT 默认关闭，当前先使用时域统计特征；若后续要启用 CMSIS-DSP FFT，需要单独补库链接并复测内存/CPU 占用。
- 经验仅供参考，不视为绝对结论。

## 2026-05-12 方案修正：相位接管前的“频率稳定”判据

### 问题修正
- 原方案文字中的 `3 秒窗口 RMS <= 30°` 容易被实现成“相对 0° 的绝对相位 RMS”。
- 实测调试证明这会导致固定相位偏置场景无法进入相位接管：相位明明稳定，但因为还没有归零，所以绝对误差 RMS 始终大于 `30°`。

### 当前判据
- 频率稳定等待阶段使用“窗口内相位波动 RMS”：
  - 窗口第一个有效重心相位作为参考点。
  - 后续每个 block 计算相对该参考点的主值相位差。
  - 3 秒内该相位差 RMS 小于等于 `30°`，才进入调制预检查和相位接管流程。
- 相位归零阶段继续使用“绝对相位误差”：
  - `phase_error_urad = atan2(Q, I)`。
  - 目标仍为 `0°`，即 IQ 重心落在 `+I` 轴。

### 调试解释
- `phase_rms_urad` 现在表示 3 秒内相位稳定程度，不再表示离 `0°` 多远。
- `phase_error_urad` 仍表示离 `0°` 多远，是相位环接管后的控制输入。

## 2026-05-12 方案回退：取消相位闭环接管

### 当前执行方案
- 已按用户要求回退到无相位闭环版本。
- Signal 主流程恢复为：`扫频锁定 -> IQ 残余频偏分析 -> OCXO VRFE 频率闭环`。
- 当前不再执行：`调制初识别 -> 相位归零闭环 -> 调制复核`。

### 工程状态
- 不删除相位闭环源码文件，避免破坏备份和后续复盘。
- `CMakeLists.txt` 不再编译 `app_phase_align.c` 和 `app_mod_detect.c`。
- `app_signal_detect.c` 不再包含相位闭环头文件，也不再调用相位接管接口。
- `app_carrier_sync.c/.h` 仅保留频率闭环接口 `app_carrier_sync_update()`。

### 后续建议
- 若后续重新设计相位锁定，建议不要直接恢复固定步进相位环。
- 更稳妥的方向是先把“频率环”和“相位环”的控制状态拆开观测，再设计小带宽 PLL 或 PI 控制，并明确相位误差、频率误差和 VRFE 输出之间的控制优先级。

## 2026-05-12 方案微调：频率闭环参数解耦

### 调整原因
- 原 `APP_CARRIER_SYNC_UPDATE_PERIOD_MS` 同时影响执行间隔和等效积分速度，参数语义不清晰。
- 频率闭环应区分：
  - 控制计算多久执行一次。
  - VRFE 电压每秒最多允许变化多少。

### 当前定义
- `APP_CARRIER_SYNC_FREQ_UPDATE_PERIOD_MS`
  - 频率闭环执行节拍。
  - 默认 `20 ms`。
- `APP_CARRIER_SYNC_FREQ_SLEW_UV_PER_SEC`
  - 频率闭环积分斜率。
  - 默认 `5000 uV/s`，即 `5 mV/s`。
- 实际单次步进由公式得到：
  - `step_uv = APP_CARRIER_SYNC_FREQ_SLEW_UV_PER_SEC * elapsed_ms / 1000`

### 调参建议
- 想让闭环更快：优先增大 `APP_CARRIER_SYNC_FREQ_SLEW_UV_PER_SEC`。
- 想让 DAC 输出更平滑：减小 `APP_CARRIER_SYNC_FREQ_UPDATE_PERIOD_MS`，但不要靠周期改变闭环速度。
- 想降低 CPU/串扰风险：增大 `APP_CARRIER_SYNC_FREQ_UPDATE_PERIOD_MS`，同时按需要保持或调整斜率。

## 2026-05-12 方案更新：频率闭环改为 PI

### 当前控制结构
- 当前频率闭环已从“符号限速积分”改为 PI 控制。
- 输入仍为 `app_iq_preproc_result_t.residual_freq_millihz`。
- 输出仍为 DAC1 对 OCXO VRFE 的控制电压 `control_uv`。

### PI 对应关系
- P 项：
  - `p_term_uv = APP_CARRIER_SYNC_CONTROL_POLARITY * residual_freq_millihz * APP_CARRIER_SYNC_FREQ_KP_UV_PER_HZ / 1000`
  - 作用：频偏越大，即时修正越大。
- I 项：
  - `i_term_uv += APP_CARRIER_SYNC_CONTROL_POLARITY * residual_freq_millihz * APP_CARRIER_SYNC_FREQ_KI_UV_PER_HZ_S * elapsed_ms / 1000000`
  - 作用：残余频偏持续不为 0 时慢慢消除静差。
- 目标电压：
  - `target_uv = APP_CARRIER_SYNC_CENTER_UV + p_term_uv + i_term_uv`
- 实际输出：
  - `control_uv` 不直接跳到 `target_uv`，而是受 `APP_CARRIER_SYNC_FREQ_SLEW_UV_PER_SEC` 限速逼近目标。

### 当前不是 PID
- 当前没有 D 项。
- 原因：`residual_freq_millihz` 本身有测量噪声，D 项容易放大抖动；OCXO VRFE 是慢对象，先用 PI 更稳妥。

### 调参顺序建议
- 先确认 `APP_CARRIER_SYNC_CONTROL_POLARITY` 方向正确。
- 再调 `APP_CARRIER_SYNC_FREQ_KP_UV_PER_HZ`，观察响应速度和是否过冲。
- 再调 `APP_CARRIER_SYNC_FREQ_KI_UV_PER_HZ_S`，消除长期静差。
- 最后用 `APP_CARRIER_SYNC_FREQ_SLEW_UV_PER_SEC` 限制 DAC 电压变化速度，保护 OCXO 不被快速拉动。

## 2026-05-12 方案回退：恢复用户实测较好的 VRFE 频率闭环版本

### 当前采用方案
- 已按用户说明回退到此前反馈“参数已经很好”的 VRFE 闭环版本。
- 当前不采用 PI，也不采用相位闭环接管。
- 当前只按 `residual_freq_millihz` 的正负号对 OCXO VRFE 做 uV 步进频率闭环。

### 当前参数
- `APP_CARRIER_SYNC_CENTER_UV = 1590000`
- `APP_CARRIER_SYNC_MIN_UV = 1400000`
- `APP_CARRIER_SYNC_MAX_UV = 1700000`
- `APP_CARRIER_SYNC_UPDATE_PERIOD_MS = 0U`
- `APP_CARRIER_SYNC_DEADBAND_MHZ = 0`
- `APP_CARRIER_SYNC_STEP_UV = 100`
- `APP_CARRIER_SYNC_CONTROL_POLARITY = 1`

### 控制关系
- 若 `residual_freq_millihz > 0`，按 `APP_CARRIER_SYNC_CONTROL_POLARITY` 指定方向调整 `100uV`。
- 若 `residual_freq_millihz < 0`，反方向调整 `100uV`。
- `APP_CARRIER_SYNC_UPDATE_PERIOD_MS = 0U` 时，每次锁定后 IQ 分析成功并调用闭环函数，都会允许一次判断和步进。

### 后续边界
- 若后续继续做相位锁定，应在这个稳定频率闭环版本之外单独设计，不再直接把 PI 或相位接管叠回当前版本。

## 2026-05-12 方案补充：锁定后每秒输出 IQ 幅度与相位

### 当前目标
- 在频率锁定并进入 IQ 跟踪后，串口每秒输出一行观测日志，用于判断 I/Q 重心、相位角和闭环稳定性。

### 观测定义
- `i_amp = abs(mean_i)`：I 路去中心后重心分量的幅度。
- `q_amp = abs(mean_q)`：Q 路去中心后重心分量的幅度。
- `phase = atan2(mean_q, mean_i)`：由 I/Q 两路重心求得的相位。
- 同时输出：
  - `ph_urad`：相位，单位微弧度。
  - `ph_mdeg`：相位，单位毫度。
  - `mag`：I/Q 重心合成幅度。

### 日志格式
- `iq_amp,i_amp=...,q_amp=...,i_mean=...,q_mean=...,mag=...,ph_urad=...,ph_mdeg=...,blk=...`

### 边界说明
- 该改动只增加观测日志，不改变扫频、频率闭环、DAC/VRFE 输出策略。
- 串口必须处于 `UART_MODE_LOG`，且频率锁定跟踪有效时才输出。
- 该日志用于调试观察，不作为当前闭环控制输入。
## 2026-05-13 相位闭环方案变更记录：从固定步进改为基准电压 DPLL

### 背景

- 目标：频率闭环稳定后，以 I 正幅度为 x 正半轴的 IQ 坐标系进行绝对相位闭环。
- 当前调试目标暂选 90 度，即 +Q 轴；原因是 0 度附近容易出现正负跨越和符号误判，90 度更便于先验证 VRFE 相位可控性。
- 前提门控保持不变：扫频锁定、`carrier_present=1`、残余频偏稳定后才允许相位接管。

### 方案变化

1. 测量方式
   - 从重心相位改为逐点相位圆均值。
   - 实现上不对每个点直接调用 `atan2f()`，而是把每个有效点归一化成单位向量后累加，最后用一次 `atan2f()` 得到平均相位。
   - 这样可以正确处理相位跨越，同时计算量可由 `APP_CARRIER_PHASE_SAMPLE_STEP` 控制。

2. 控制方式第一版：固定步进
   - 直接根据相位误差正负号，每次改变 DAC 若干 uV。
   - 实测问题：接近目标后不减速，OCXO 频率偏置继续积分成相位漂移，导致穿过目标后继续转。

3. 控制方式第二版：继承频率闭环电压作为基准
   - 相位接管瞬间记录 `phase_base_uv = control_uv`。
   - 相位环只在 `phase_base_uv` 附近施加 `phase_trim_uv_q` 小数 uV 修正，不再无边界叠加当前 DAC。
   - 实测收益：`uv` 不再大幅扫动，ADC 实时性未恶化。
   - 实测不足：初始修正太弱，不能抵消已存在的相位速度。

4. 控制方式第三版：加入相位速度阻尼
   - 新增 `phase_delta_mdeg` 表示相邻有效 block 的相位误差变化。
   - `phase_control_mdeg = phase_error_mdeg + APP_CARRIER_PHASE_DPLL_RATE_LEAD * phase_delta_mdeg`。
   - 用 `phase_control_mdeg` 更新内部小数 uV 修正量，再限幅到每次最大 `APP_CARRIER_PHASE_DPLL_MAX_STEP_UV`。
   - 实测状态：方向有改善迹象，但尚未稳定锁住目标。

### 当前参数与对应现象

- `APP_CARRIER_PHASE_TARGET_MDEG=90000`：当前锁 90 度。
- `APP_CARRIER_PHASE_SAMPLE_STEP=16`：每个 4096 点 block 抽约 256 点，日志中 `u=256`。
- `APP_CARRIER_PHASE_MIN_RESULTANT_PM=150`：当前 CW 下日志 `rel=1000`，说明相位测量质量充分。
- `APP_CARRIER_PHASE_DPLL_MAX_STEP_UV=2`：保护 VRFE 不被快速拉飞。
- `APP_CARRIER_PHASE_DPLL_INTEGRAL_SCALE_MDEG_PER_UV=90000`：比 900000 版本强约 10 倍；900000 过慢，90000 仍未完全闭合。
- `APP_CARRIER_PHASE_DPLL_RATE_LEAD=128`：用于提前制动；当前仍需继续根据日志调试。

### 后续建议

- 不再盲目增加固定步进；相位环需要围绕“相位速度是否减小”调参。
- 下一步优先增加更细的状态观测，例如每 100ms 输出一次相位 DPLL 摘要，或通过 STLINK 直接观察 `g_carrier_sync_status_dbg.phase_delta_mdeg`、`phase_control_mdeg`、`phase_delta_uv`。
- 若 `phase_delta_mdeg` 长期同号且目标被反复穿过，应继续增大阻尼或翻转/修正控制极性。
- 若 `uv` 长期贴近 `phase_base_uv ± APP_CARRIER_PHASE_DPLL_TRIM_LIMIT_UV`，说明相位环修正范围不够或频率闭环交接点偏离，需要先重新评估频率闭环稳态。

## 2026-05-13 02:40 相位闭环调参结论补充

### 方案判断

- 已验证：相位目标 90 度、逐点相位圆均值、频率稳定后相位接管这条主线可运行。
- 已验证：相位 DPLL 必须保留 residual 频偏制动项；只看相位误差会让相位继续以几十度/秒滑动。
- 已验证：`2uV` 最大步进过小，捕获能力不足；`5uV` 最大步进过猛，过冲更明显；当前折中为 `3uV`。

### 当前阶段性方案

- 相位闭环目标仍为 90 度，先把“能否通过 VRFE 让 IQ 绝对相位停住”验证清楚。
- 控制量采用：

```text
phase_control_mdeg =
    phase_error_mdeg
  + APP_CARRIER_PHASE_DPLL_RATE_LEAD * phase_delta_mdeg
  + APP_CARRIER_PHASE_DPLL_RESIDUAL_GAIN * residual_freq_millihz
```

- 当前推荐基线：
  - `APP_CARRIER_PHASE_DPLL_RATE_LEAD=128`
  - `APP_CARRIER_PHASE_DPLL_RESIDUAL_GAIN=1536`
  - `APP_CARRIER_PHASE_DPLL_MAX_STEP_UV=3`
  - `APP_CARRIER_PHASE_DPLL_INTEGRAL_SCALE_MDEG_PER_UV=90000`

### 未完成项

- 当前仍是实验闭环，尚未达到严格“每次复位后稳定锁在 90 度”的完成标准。
- 后续调参方向不再盲目加大步进，而是围绕 `abs_deg_avg`、`abs_deg_max`、`abs_rf_avg` 三个指标评估。
- 如果要用于比赛运行，需在验证完成后把 100ms carrier 日志降回 1s 或关闭高频日志。

## 2026-05-13 03:45 相位 DPLL 方案补充：禁止双任务叠加，改为单写入者合成控制

### 方案判断

- 不采用“频率闭环任务 + 相位闭环任务分别计算 DAC，最后相加”的结构。
- 原因：两个实时路径同时修改同一 VRFE 控制量，容易形成竞态和互相抵消；即使用锁保护，也会让 ADC block 时序和调参含义变复杂。
- 当前坚持一个 DAC 写入者：`app_carrier_sync_update_iq()` 内部判断模式，最终只通过 `app_carrier_sync_apply_uv()` 写一次 DAC。

### 本轮调整

- 相位模式继续继承频率闭环最后的 `control_uv`，不回中心。
- residual 频偏先低通为 `phase_freq_lpf_millihz`，再作为相位模式下的频率制动项。
- 相位环极性改为 `APP_CARRIER_PHASE_CONTROL_POLARITY=-1`；该结论来自串口实测，`+1` 会把相位误差继续推离 90°。
- 串口新增 `carx` 行，用于判断相位模式是否真的在减速、是否触发目标附近制动、是否存在频偏重新变大的问题。

### 本轮结论

- 已验证：相位计算可靠，接管状态可观测，ADC 未见堆积，极性翻转后可接近 90°。
- 未解决：接近目标后 residual 仍可能重新增大，导致相位继续绕圈。
- 下一步不建议继续盲目调大/调小步进；应先增加“相位模式频偏保护退出”或改成更明确的二状态 PLL：

```text
频率闭环压 residual -> residual 稳定窗口满足
进入相位闭环 -> 只允许小范围相位微调
若相位模式 residual 重新变大 -> 退出相位闭环，回到频率闭环
```

## 2026-05-13 0 度相位锁定方案：目标 residual 轨迹环

### 方案判断

- 0 度比 90 度更符合最终解调坐标：`atan2(Q, I)=0` 时，I 路位于正半轴，Q 路接近 0。
- 直接按相位误差积分调 DAC 已验证容易绕圈；本轮改为“相位误差生成目标 residual，DAC 只负责追踪目标 residual”。
- 不采用频率环和相位环两个任务并行相加，继续保持单一 DAC 写入者，避免竞态和调参含义混乱。

### 控制流程

```text
扫频锁定
  -> 频率闭环把 residual 压到稳定门限
  -> phase_wait 计数满足
  -> phase_lock 继承当前 DAC 电压
  -> 计算绝对相位 current_phase = atan2(Q, I)
  -> phase_error = wrap(0 - current_phase)
  -> 生成小目标 residual
  -> 用 residual 低通值追踪目标 residual
  -> 接近 0 度后目标 residual 置 0
```

### 远区防抖

- 当绝对相位进入 ±170 度附近时，启用远区方向保持。
- 远区内不按瞬时 `+179/-179` 符号翻转方向，而是沿上一有效方向继续推一小段。
- 相位离开 ±150 度后恢复最短路径控制。

### 当前参数

- `APP_CARRIER_PHASE_TARGET_MDEG=0`
- `APP_CARRIER_PHASE_SETTLE_MS=6000U`
- `APP_CARRIER_PHASE_TARGET_RESIDUAL_MAX_MHZ=50`
- `APP_CARRIER_PHASE_LOCK_BAND_MDEG=2000`
- `APP_CARRIER_PHASE_RESIDUAL_DEADBAND_MHZ=5`
- `APP_CARRIER_PHASE_FAR_ENTER_MDEG=170000`
- `APP_CARRIER_PHASE_FAR_EXIT_MDEG=150000`
- `APP_CARRIER_PHASE_RESIDUAL_POLARITY=1`

### 验收观察

- `car,m=phase_lock` 后，`carp,ph` 应逐步靠近 `0.000`。
- `carx,tr` 应随 `carp,er` 变小而减小，接近目标后变为 0。
- `carx,dcd/chg` 用来判断 DAC code 是否真实变化，不能只看 `uv`。
- `carq,u/rel` 用来判断逐点相位计算是否可靠；CW 下应长期接近 `u=256,rel=1000`。

## 2026-05-13 相位锁定算法清理后的手写入口方案

### 当前代码状态

- 频率闭环保留，仍由 `app_carrier_sync_update_frequency()` 根据 `residual_freq_millihz` 调 VRFE。
- 相位闭环不再接管 DAC，`APP_CARRIER_PHASE_LOCK_ENABLE=0U`。
- `app_carrier_sync_update_iq()` 仍接收原始 IQ block，并刷新相位观测字段，后续手写相位锁定建议从该函数内重新接入。

### 后续手写建议

- 第一步只写“相位观测判据”，不写 DAC：
  - 观察 `phase_abs_mdeg` 是否稳定。
  - 观察 `phase_resultant_pm` 是否长期接近 1000。
  - 观察 `residual_freq_millihz` 是否已进入可控范围。
- 第二步写“状态机”，先不动 DAC：
  - `FREQ_LOCK`：频率闭环工作。
  - `PHASE_READY`：频率稳定、相位有效，等待接管。
  - `PHASE_ACTIVE`：允许相位算法输出控制量。
  - `PHASE_ABORT`：频偏变大或相位质量下降，退回频率闭环。
- 第三步再写 DAC 控制：
  - 不要让两个任务同时写 DAC。
  - 只允许 `app_carrier_sync` 一个模块写 `app_carrier_sync_apply_uv()`。
  - 新相位算法先输出“目标 residual”或“目标频率偏置”，再由频率误差去调 DAC。
- 第四步单独验证方向：
  - 固定输入 CW。
  - 人为记录“DAC code 增加时，相位是顺时针还是逆时针移动”。
  - 方向没确认前，不写自动锁定逻辑。

### 推荐落点

- 相位测量继续复用 `app_carrier_sync_calc_point_phase()`。
- 相位状态机建议新建小函数，例如：
  - `app_carrier_sync_phase_state_update()`
  - `app_carrier_sync_phase_target_residual()`
  - `app_carrier_sync_phase_apply_residual_target()`
- 不建议把相位控制写进 `AdcTask.c`，也不建议在 `app_signal_detect.c` 里直接调 DAC。

## 2026-05-13 频率锁定路径简化方案

### 目标

- 保持频率闭环行为不变，只降低 `app_carrier_sync_update_iq()` 的分支复杂度。
- 让“频率闭环”和“相位观测”同时存在，但不让相位算法接管 DAC。

### 当前流程

```text
locked_gate=0 或 iq_result=NULL
  -> 失锁回中心

locked_gate=1
  -> 刷新 residual 状态
  -> 计算逐点相位观测字段
  -> mode 固定为 freq
  -> app_carrier_sync_update_frequency()
  -> 根据 residual 符号小步调 DAC
```

### 后续相位代码入口

- 后续如果重新写相位锁定，不要恢复 `APP_CARRIER_PHASE_LOCK_ENABLE` 早返回结构。
- 建议在“相位观测字段刷新之后、频率闭环调用之前”插入状态机：

```text
phase_observe_update()
if phase_state == ACTIVE:
    phase_control_update()
else:
    app_carrier_sync_update_frequency()
```

- 这样相位观测、状态机、DAC 写入路径会比较清楚，不会再次出现“关掉相位闭环就看不到相位观测”的问题。

## 2026-05-14 高密度相位闭环验证方案

### 当前采用方案
- 继续采用“频率锁定后，相位模块接管 DAC”的总思路。
- 保持简单的慢速步进法，不引入 PWM，不引入 PI/PID，不让两个环同时写 DAC。
- 新增高密度串口日志，把当前验证拆成两层：
  - `car/carp`：看频率环与相位观测本身；
  - `phl/phx`：看相位环是否接管、当前方向、当前节拍、最近一次 DAC 步进。

### 本轮核心逻辑
- 相位接管条件：
  - `locked_gate = 1`
  - `phase_valid = 1`
  - 稳定窗口达到 `800 ms`
- 相位接管动作：
  - 先调用 `app_carrier_sync_manual_adjust_uv(0)`，只切换到 `manual`，不改电压。
- 相位推进方式：
  - 目标仍是 `atan2(Q, I) = 0°`
  - 误差越大，节拍越快；
  - 误差进入 `5°` 量级后，改用更慢的刹车节拍；
  - 进入 `1.2°` 死区后保持不动。
- 正负相位问题处理：
  - 默认按 `phase_error_mdeg` 的主值正负号判向；
  - 但在 `±180°` 附近先保持上一方向，避免 `atan2` 主值边界抖动导致方向来回翻转。

### 当前验证结果
- 代码、编译、烧录、串口接入链路已通。
- 当前实机串口持续显示未锁定：
  - `sig,s=2,sc=0,lk=0,cp=0,lo=46700000,...`
- 因为还没进入 `locked`，所以本轮新增的 `phl/phx` 日志暂时没有触发。

### 现阶段判断
- 现阶段还不能根据这轮实机结果判断新相位环方向是否正确，也不能判断节拍是否过快或过慢。
- 当前最直接阻塞项是：**先恢复到可稳定锁定的输入条件**。

### 下一步建议
- 先不要继续盲调相位闭环参数。
- 下一轮优先做两件事：
  1. 确认当前硬件改线后，信号源幅度、频率、接线和扫频输入链是否仍满足锁定条件；
  2. 一旦重新进入 `locked`，直接使用这版高密度日志判断：
     - 是否接管；
     - 方向是否正确；
     - 最近一次 DAC 步进是否真正跨过 DAC LSB；
     - 接近 0° 时是否明显减速。

### 经验说明
- 经验仅供参考，不视为绝对结论。
- 这版代码的目标是把“相位环内部到底在干什么”暴露清楚，而不是在未锁定前继续拍脑袋调参。






## 2026-05-14 频率锁定逻辑重写方案已落地

### 本轮目标
- 不再沿用“`deadband=0 + 固定 1000uV + 每次调用都推 DAC`”的粗糙频率环。
- 先把频率锁定逻辑收成一版简单、慢速、便于解释和便于串口观察的结构。
- 本轮验证阶段默认关闭自动相位接管，先把频率环单独跑通。

### 最终落地结构
- 输入：`residual_freq_millihz`
- 处理顺序：
  1. 对原始频偏做轻量低通，得到 `residual_filtered_millihz`
  2. 若 `|ff| <= 5mHz`，进入死区保持
  3. 否则按 `|ff|` 选择三档步进与三档节拍
  4. 到节拍点后才推进一次 DAC
- 这样做的直接收益：
  - 不再把每个 block 的瞬时抖动直接打到 OCXO VRFE
  - 锁定后步进更慢，便于后续再接相位环
  - 串口能直接看出当前是“原始频偏大”还是“滤波后频偏大”

### 三档策略
- 小误差：`|ff| < 80mHz`
  - `su = 200uV`
  - `it = 1000ms`
- 中误差：`80mHz <= |ff| < 200mHz`
  - `su = 600uV`
  - `it = 500ms`
- 大误差：`|ff| >= 200mHz`
  - `su = 1200uV`
  - `it = 250ms`

### 极性复测结论
- `APP_CARRIER_SYNC_CONTROL_POLARITY = +1` 时，DAC 被推到上边界，`rf/ff` 仍在约 `5Hz` 附近，方向不对。
- 改为 `APP_CARRIER_SYNC_CONTROL_POLARITY = -1` 后，`rf/ff` 降到 `0.1Hz` 量级，且可进入 `5mHz` 死区附近。
- 因此当前工程主线应保留：`APP_CARRIER_SYNC_CONTROL_POLARITY = -1`。

### 当前建议
- 先以这版频率环作为新的主线基线。
- 后续若继续做相位锁定，应当在这版频率环之上接入，不要再把相位环和频率环同时写 DAC。
- 相位环恢复前，先继续观察：
  - `car,rf/ff`
  - `uv`
  - `dc/dcd/chg`
  确认频率环在不同输入幅度和不同锁定起点下都不会再跑满边界。

### 2026-05-14 单档调参说明
- 频率环不再按误差大小切三档。
- 当前结构改为：
  - 低通频偏
  - 死区判断
  - 单档固定步进 `APP_CARRIER_SYNC_TUNE_STEP_UV`
  - 单档固定节拍 `APP_CARRIER_SYNC_TUNE_INTERVAL_MS`
- 这样后续调参时只需要动两个宏，不再需要同时判断当前落在哪个档位。
- 2026-05-14 频率环按用户要求恢复为三档结构，不再保持单档调参版本。

### 2026-05-14 Sweep 可读性重构
- 问题背景：`app_signal_sweep.c` 的 coarse/fine 启动链和阶段完成链函数跳转较多，虽然职责清晰，但顺着代码读主流程时来回跳转成本高。
- 本轮方案：
  - 保持 `Sweep` 对外接口不变；
  - 仅收口内部命名和阶段主链；
  - 不改 sweep 判据、不改 DDS 下发路径、不改 `Pipeline` 调用顺序。
- 落地结果：
  - coarse 入口统一为 `app_signal_sweep_start_coarse_scan()`；
  - coarse/fine 阶段装载统一为 `app_signal_sweep_start_stage()`；
  - 阶段结束统一从 `app_signal_sweep_complete_stage()` 进入；
  - 最终 locked 落点仍由 `app_signal_sweep_finish_locked()` 处理。
- 保留项：
  - `app_signal_sweep_begin_fine_scan()` 继续保留为 fine 窗口 helper；
  - `app_signal_sweep_eval_current_sweep()` 继续保留为 sweep 曲线分析函数；
  - trace/log 逻辑仍在同文件中，避免这轮重构扩大影响面。
- 当前收益：
  - `init/request_rescan/process_block` 到 sweep 内部主链的入口名称更直接；
  - 主流程更接近“coarse 启动 -> block 推进 -> 阶段完成 -> fine 或 locked”。

### 2026-05-14 Sweep 主流程二次收口方案落地
- 目标从“只收入口名称”推进到“主流程、分析流程、日志旁路”三层可读性同时收口。
- 当前 sweep 阅读路径已压缩为：
  - pp_signal_sweep_start_coarse_scan()：统一 coarse 入口；
  - pp_signal_sweep_start_stage()：统一 coarse/fine 阶段装载；
  - pp_signal_sweep_process_block()：只负责 block 推进和 step 结束判断；
  - pp_signal_sweep_complete_stage()：统一决定继续 fine 还是提交最终 locked 结果。
- pp_signal_sweep_eval_current_sweep() 现已按固定判据顺序组织，避免原先大块条件分支阅读时来回跳：
  1. 基础有效性检查；
  2. rise 与阈值计算；
  3. 激活区左右边界搜索；
  4. 谷值优先或边界中点估计；
  5. 结果写回。
- trace/log 继续留在同一 .c 文件，但实现放到文件尾部，确保主流程先读算法，再读日志旁路。
- 本轮没有改 coarse/fine 判据、没有改 fine 窗口算法、没有改 DDS 下发路径，属于纯内部结构和命名清理。

### 2026-05-14 注释规范知识库同步
- 已将“函数头注释分级规则”写入外部 AI 知识库《嵌入式代码注释习惯与落地规则.md》。
- 本轮没有发现规则层面的冲突；唯一问题是原文件编码已乱码，因此已整体整理为 UTF-8 正常版本后再合并新规则。
- 新增规则核心：
  - 对外接口函数必须写完整 @brief/@param/@return；
  - 重要内部函数写完整函数头注释；
  - 很短且语义一眼可见的 helper 只写 @brief，避免机械注释把文件重新写胖。

### 2026-05-14 手动微调链路清理方案落地
- 用户要求删除“手动微调 DDS CH0 相位”和“手动微调 OCXO VRFE 控制电压”的相关逻辑代码和兼容接口。
- 当前处理方式：
  1. 完全移除 UI 和 Signal Detect/Pipeline 的用户侧入口；
  2. 保留相位闭环自动调压所需的 DAC 微调能力，但改名为相位闭环内部专用接口；
  3. 日志中的控制模式名称同步从 manual 改为 phase，让当前主线语义与代码职责一致。
- 这样处理后，用户侧已经无法再通过屏幕或兼容接口触发 DDS/OCXO 手动微调，但自动相位闭环主线保持不受影响。

### 2026-05-15 锁定态调度链再收口
- 用户要求进一步降低锁定态主链的阅读成本，避免“phase 是否启用”和“是否接管 DAC”在主链外再套多级判断。
- 本轮方案调整为：
  1. `locked_block` 主链只保留同级顺序：`IQ 结果 -> carrier 观测 -> phase 判定 -> frequency loop 是否执行 -> 日志输出`；
  2. `phase` 是否启用与是否接管统一由 `app_signal_pipeline_process_locked_phase()` 内部处理；
  3. `carrier` 观测函数直接返回最新 `carrier_status`，减少一次额外快照获取；
  4. 删除只做单层转发的 `process_locked_control()` 薄包装。
- 本轮不改频率环和相位环的控制参数，只改锁定态调度关系与判定落点。

### 2026-05-15 phase_align 控制流再收口
- 用户要求继续贯彻“逻辑扁平化”，本轮继续处理 `app_phase_align_update()` 内部的判定分散问题。
- 方案调整为：
  1. 主函数只保留三步：同步观测 -> 接管前门控 -> 接管后控制；
  2. 将“相位无效时等待 / 稳定窗口累计 / 建立接管”集中到 `app_phase_align_prepare_takeover()`；
  3. 将“死区保持 / 节拍限速 / 方向判定 / DAC 推进”集中到 `app_phase_align_run_active_control()`；
  4. 观测字段同步和 `phase_delta` 计算单独抽成 helper，避免主函数同时堆叠状态搬运和业务判定。
- 本轮不改业务参数，只整理控制流层级，便于后续继续改接管门控或控制律。

### 2026-05-15 carrier_sync 观测层再收口
- 用户继续要求主链与算法文件内部保持扁平化，本轮对 `app_carrier_sync_update_iq()` 做同类收口。
- 方案调整为：
  1. 失锁路径集中到 `app_carrier_sync_handle_unlocked_state()`；
  2. 锁定帧的残余频偏同步集中到 `app_carrier_sync_sync_locked_observation()`；
  3. 块内相位观测刷新集中到 `app_carrier_sync_refresh_phase_observation()`；
  4. `app_carrier_sync_on_unlock()` 直接走失锁收口逻辑，不再经由主线观测入口转发。
- 为避免联动扩大，本轮保留 `app_carrier_sync_update()` 作为兼容薄入口，但当前锁定态主链与失锁主链都不再依赖它来理解流程。

### 2026-05-15 旧兼容层清理与主链同级化
- 用户明确要求删除没用的旧兼容入口，并减少“简单函数套简单函数”的跳转层级。
- 本轮执行策略：
  1. 直接删除 `app_signal_detect.c/.h` 兼容层，而不是继续保留转发壳；
  2. 所有直接依赖点改为调用 `app_signal_pipeline` 新主入口；
  3. `locked` 主链内只保留真正有业务意义的阶段边界，不再保留只被单次调用的薄包装函数；
  4. `carrier_sync` 中去掉 `app_carrier_sync_update()` 这类已经失去主线价值的兼容入口。
- 当前结果：
  - `Signal` 主线已经统一到 `app_signal_pipeline`；
  - `locked` 主链现在是显式同级顺序，而不是通过多层 helper 才看明白谁先跑、谁后跑；
  - `carrier_sync` 的失锁收口和锁定观测同步重新回到 `app_carrier_sync_update_iq()` 本体，减少跳转成本。

### 2026-05-15 锁定门控与状态快照继续收口
- 用户进一步要求：像 `app_signal_pipeline_build_locked_gate()` 这种既不复杂、也不复用、也不跨文件的逻辑，不要再单独包函数。
- 本轮调整策略：
  1. 将 locked/unlocked 分支判断直接收回 `app_signal_pipeline_process_block()`；
  2. 将 `carrier` 和 `phase` 的状态快照直接作为更新函数的可选输出参数回传；
  3. 删除只负责“调一次、再 get 一次状态”的薄接口，避免主链阅读必须跨层来回确认。
- 当前结果：
  - `locked_gate` 的构造与使用已经在 `pipeline` 主入口同级可见；
  - `carrier_sync` 与 `phase_align` 不再要求主链先调用更新函数、再额外调用状态 getter 才能继续判定；
  - `phase` 是否接管、本帧是否执行频率环，现在都能在 `pipeline` 主链一屏内读清楚。
  - `iq_track` 旧快照 getter 已删除，快照随 `process_locked_block()` 直接输出，避免继续保留无调用死接口。
## 2026-05-16  算法日志细分开关与 VOFA 相位单通道输出

- 背景：
  - 现场需要暂时只观察 `iq_amp` 中的 `ph_deg` 相位波形，避免被其他算法日志刷屏。
- 本次调整：
  - 在 `AppDebugConfig.h` 中细分日志控制宏：
    - `ADC_SIG_LOG_ENABLE`
    - `ADC_IQ_PRE_LOG_ENABLE`
    - `ADC_IQ_ROT_LOG_ENABLE`
    - `ADC_IQ_AMP_PHASE_LOG_ENABLE`
    - `ADC_IQ_AMP_PHASE_VOFA_ENABLE`
    - `ADC_CARRIER_SYNC_LOG_ENABLE`
    - `ADC_PHASE_ALIGN_LOG_ENABLE`
  - 默认只开启 `ADC_IQ_AMP_PHASE_LOG_ENABLE=1U` 与 `ADC_IQ_AMP_PHASE_VOFA_ENABLE=1U`，其余相关算法日志默认关闭。
  - `app_adc_log_iq_amp_phase_1s()` 在 `UART_MODE_VOFA` 下改为输出 `ph_deg:<value>\n`，符合 FireWater 单通道格式。
- 结果：
  - 当前可在 VOFA 中直接观察单条 `ph_deg` 波形。
  - 若后续需要恢复某类文本日志，只需在 `AppDebugConfig.h` 打开对应宏即可。

## 2026-05-16  VOFA 相位输出扩展为三通道

- 背景：
  - 现场需要把 `ph_deg`、当前 DAC 控制电压以及 `APP_PHASE_KP_UV_PER_DEG` 同时送入 VOFA 画波形。
- 本次调整：
  - `app_adc_log_iq_amp_phase_1s()` 增加 `carrier_status` 输入。
  - `UART_MODE_VOFA` 下输出格式改为：
    - `ph_deg:<phase_deg>,<control_uv>,<kp_uv_per_deg>\n`
  - 将 `iq_amp` 的调用点后移到 carrier/phase/freq 处理完成后，保证输出的 `control_uv` 是当前链路最终状态。
- 结果：
  - VOFA/FireWater 可直接接收 3 个通道：
    - 相位角 `ph_deg`
    - 当前 DAC 逻辑控制电压 `control_uv`
    - 当前相位环比例系数 `APP_PHASE_KP_UV_PER_DEG`

## 2026-05-16  VOFA 三通道改为电压单位与 step_uv

- 背景：
  - 现场希望第三通道直接观察当前步进，不再看固定比例系数。
  - 同时第二通道希望使用电压单位，而不是微伏整数值。
- 本次调整：
  - `app_adc_log_iq_amp_phase_1s()` 改为接收 `phase_status`。
  - `UART_MODE_VOFA` 下输出格式改为：
    - `ph_deg:<phase_deg>,<dac_voltage_v>,<step_uv>\n`
  - 第二通道输出 `control_uv` 对应的电压字符串。
  - 第三通道输出当前步进：
    - 相位模式下取 `phase.last_delta_uv`
    - 频率模式下取 `carrier.current_step_uv`


# 2026-05-17 相位环去保持区/去死区方案

- 目标：彻底删除相位闭环中导致平台段的保持区、死区和过零刹车逻辑。
- 方案：
  - 删除 `APP_PHASE_HOLD_EXIT_MDEG`、`APP_PHASE_HOLD_INTERVAL_MS`、`APP_PHASE_HOLD_MAX_STEP_UV`。
  - 删除 `APP_PHASE_ALIGN_STAGE_HOLD` 状态。
  - 相位接管后统一只走主动控制节拍 `APP_PHASE_ALIGN_INTERVAL_MS` 和步进限幅 `APP_PHASE_MAX_STEP_UV`。
  - 删除日志和运行模式判断里对 `HOLD` 状态的依赖。
- 结果：
  - 相位环控制链更扁平，平台段只会来自步进限幅/节拍本身，不再来自保持区门控。
  - 已重新编译验证通过。
# 2026-05-17 相位环步进线性化方案

- 目标：去掉相位环内部对 `P/I/D` 输出的最小步进、最大步进和额外补偿，保证相位误差到步进输出尽量保持线性。
- 方案：
  - `phase_calc_delta_uv()` 直接输出 `raw_uv`。
  - `pid_limited_uv` 改为同步等于 `pid_raw_uv`，用于调试观察真实控制输出。
  - 保留硬件层的 DAC 电压上下限和 DAC 码值量化，不在相位环内部再做二次非线性整形。
- 结果：
  - `P` 项日志和相位环候选步进输出不再出现由相位环内部限幅/最小步进造成的平台。
  - 当前剩余非线性来源仅在 DAC 硬件层和物理对象本身。
# 2026-05-17 相位环到 DAC 一比一映射方案

- 目标：去掉“相位环输出作为增量叠加到当前 DAC 电压”的积分式传递，改为直接映射。
- 方案：
  - `phase_align` 继续输出线性 `offset_uv`。
  - `carrier_sync` 在相位模式下直接执行 `APP_CARRIER_SYNC_CENTER_UV + offset_uv`。
  - 不再以当前 `control_uv` 为基准做累加。
- 结果：
  - 当前相位环输出与 DAC 目标偏移建立一比一关系。
  - 剩余非线性仅来自 DAC 上下限和 DAC 码值量化。
# 2026-05-17 相位模块降级为纯观测方案

- 目标：清空相位闭环，只保留可复用的相位观测能力和独立 DAC 输出函数。
- 方案：
  - `app_phase_align_update()` 不再接管 DAC，不再运行 P/I/D。
  - 仅保留：
    - IQ 重心相位误差计算
    - 绝对相位计算
    - 相位变化量与变化速度计算
  - `carrier_sync_phase_adjust_uv()` 继续保留为独立 DAC 输出入口，但不再由 `phase_align` 主线自动调用。
  - `signal_ctrl` 取消对相位接管状态的运行模式依赖。
- 结果：
  - 相位模块主线逻辑显著扁平化。
  - 当前工程里相位相关代码只承担观测职责，不再承担闭环控制职责。
# 2026-05-17 相位模式空占位接管方案

- 目标：保持“先频率闭环、后进入相位模式”的主链结构，但暂时不执行相位闭环控制。
- 方案：
  - `phase_align` 继续做相位观测。
  - 满足稳定门控后调用 `app_carrier_sync_enter_phase_mode()` 切入 `PHASE`。
  - 进入后不再退出，不再执行任何相位 DAC 调节，只保留空占位函数。
- 结果：
  - `carrier.mode` 和 `phase.takeover_active` 仍能体现相位模式接管。
  - 频率环在相位模式下继续按现有主链旁路。
# 2026-05-17 相位接管同帧执行闭环函数方案

- 目标：满足相位接管条件后，当前 block 内立即进入用户闭环函数，避免“本帧只置位、下一帧才控制”的延迟。
- 方案：
  - `app_phase_align_update()` 中保留 `phase_try_enter_mode()` 作为接管门控。
  - 门控执行后立刻再次检查 `takeover_active`。
  - 只要 `takeover_active != 0`，当前帧直接调用 `phase_active_placeholder()`。
- 结果：
  - `phase_active_placeholder()` 成为明确的相位闭环执行入口。
  - 后续用户在该函数内写控制律时，满足门控后可以立即运行。

# 2026-05-17 扫频专用退化方案

- 目标：
  - 工程退化为只完成扫频并返回中心频率。
  - 不再保留锁频、相位、IQ 跟踪、调制识别、解调和算法日志链路。
- 方案：
  - 新建 APP 层 `Core/App/Sweep/app_sweep.c/.h`，扫频模块只公开 `app_sweep_find_center_hz(...)`。
  - 对外调用时传入起点、终点、粗扫步长和当前 ADC I/Q 数据块。
  - 模块内部按粗扫结果自动进入细扫，细扫窗口为粗步长左右各 2 倍，细扫步长为粗步长的 1/6。
  - 扫频强度采用 I/Q 两路 Vpp 的较大值。
  - DDS 频点切换继续走 `AppDDS_MakeSetChFreqApplyCmd()` 和 `AppDDS_DispatchCmd()`。
  - `AdcTask` 只做 ADC 块领取、cache 处理和信号量释放；算法调度由复用后的 `ModDetectTask` 承担。
- CubeMX 约束：
  - `SweepBlockReadySem` 已由用户创建。
  - APP 层只引用 `SweepBlockReadySemHandle`，不手改 `Core/Src/freertos.c`。
- 删除策略：
  - 按用户确认，`Core/App/LOG` 与 `Core/App/Signal` 已物理清空。
  - 删除动作不使用递归删除和通配删除。
- 结果：
  - 当前活动 APP 主线只剩 ADC 数据块发布、扫频调度、DDS 频点切换和 UI 显示。
  - Debug 构建通过。

# 2026-05-17 PrintQueue 单宏控制方案

- 目标：
  - 串口输出不再区分 `log` 和 `vofa` 类型。
  - 只用一个宏控制是否允许日志输出，宏开启后所有已投递到 `PrintQueue` 的文本都允许发出。
- 方案：
  - 在 `AppDebugConfig.h` 只保留 `APP_PRINT_LOG_ENABLE` 作为总开关。
  - `RtosTypes.h` 删除 `uart_mode_t`、`print_kind_t` 和 `g_uart_mode`，`print_msg_t` 删除类型字段。
  - `PrintfTask` 只做队列取出和 UART 发送，不再根据消息类型过滤。
  - 其他 APP 调用点统一改为 `print_queue_send()` 或无类型的 `PrintQueue` 投递。
- 结果：
  - 当前串口输出链路只有“开/关”两种状态。
  - `APP_PRINT_LOG_ENABLE=1U` 和恢复默认 `0U` 两种构建状态均通过。
  - 旧类型分流相关符号在活动 APP 源码中已清理。
# 2026-05-21 板载 QSPI Flash 隔离开发方案

- 目标：
  - 在当前测试工程中引入 FK743M5-XIH6 板载 W25Q64 QSPI Flash 基础能力。
  - 不把自测接入任何主线任务，避免影响当前无线 IQ 主线流程。
- 方案：
  - 新增 `Core/App/BoardFlash` 三层封装：应用 API、W25Q64 指令层、QSPI HAL 适配层。
  - 补齐 `quadspi.c/h`，启用 HAL QSPI 驱动并加入 CMake。
  - V1 使用标准一线 Flash 指令完成读写擦，后续确认硬件稳定后再考虑 Quad I/O、memory-mapped 和文件系统。
  - 自测必须显式传入 scratch 地址，禁止模块内部自动选择擦除区域。
- 结果：
  - Debug 构建通过。
  - 尚未进行上板 JEDEC ID 和 scratch sector 自测。
