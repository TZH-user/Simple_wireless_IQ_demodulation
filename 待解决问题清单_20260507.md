# 待解决问题清单（2026-05-07）

## 说明
- 本文件只记录“已确认但暂不处理”的问题。
- 当前状态：先理解算法与链路，不立即修改。
- 处理原则：后续逐项审查、逐项验证，不一次性大改。

## A. 算法问题

### A1. `ci` / `cq` / `mag2` 被重复计算
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：第一遍循环和第二遍循环都重复计算 `ci`、`cq`、`mag2`。
- 影响：增加乘法和加法次数，浪费算力。
- 当前判断：属于性能优化问题，不是当前主风险。
- 后续方向：可考虑“第一遍缓存，第二遍复用”，或后续重新设计特征提取流程。

### A2. `env_min` / `env_max` 对单点坏样本敏感
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：整块直接取最小值/最大值，容易被偶发 ADC 毛刺或坏点拉偏。
- 影响：会连带影响 `env_span`、`env_mid`、`env_hyst`、`env_edge_threshold`、`am_depth_pm` 等多个量。
- 当前判断：高优先级风险，尤其影响 AM / ASK 类判断。
- 后续方向：优先考虑 `mag2` 先做 3 点中值窗，再参与极值/边沿统计；备选为裁剪极值或百分位方案。

### A3. `env_edge_threshold` 为经验式阈值
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：`env_edge_threshold = max(env_span/8, max(mean_power/64, 1))`。
- 影响：能工作，但物理意义弱，跨场景迁移性一般。
- 当前判断：中优先级问题。
- 后续方向：后续结合实测数据决定是否替换为更稳的包络边沿阈值策略。

### A4. `dot < 0` 作为 PSK 跳变判据过粗
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：当前只把“相邻 IQ 向量夹角超过 90 度”记为一次 `psk_jump`。
- 影响：只能粗略表示大相位跳变，不能严格代表 PSK 符号翻转。
- 当前判断：高优先级问题。
- 后续方向：后续考虑基于 `Δφ[n] = angle(z[n] * conj(z[n-1]))` 的更正规相位增量分析。

### A5. `dphi_hist` 仅 5 桶粗分
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：只按 `cross` 的符号和强弱粗分成 5 个桶。
- 影响：相位分辨率较低，对 PSK / FSK 的区分能力有限。
- 当前判断：中优先级问题。
- 后续方向：后续再决定是否升级为更细粒度直方图或直接做相位序列分析。

### A6. `am_depth_pm` 直接由极值推导
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：`am_depth_pm` 直接依赖 `env_min` 和 `env_max`。
- 影响：会继承极值污染问题，显示值可能被单点异常拉偏。
- 当前判断：高优先级问题。
- 后续方向：等包络极值策略稳定后再一起修正。

### A7. 参数估计存在经验缩放系数
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：AM / ASK / FM / FSK / PSK 的参数输出使用了 `2/5`、`1/3`、`4/5`、`1/17`、`2/11` 等经验系数。
- 影响：显示参数可用，但解释性弱，不适合直接当高可信物理量。
- 当前判断：高优先级问题。
- 后续方向：后续需要基于测试矩阵重新校准，或更换参数估计方法。

### A8. 调制分类树可读性差
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：判定树依赖多个长变量和组合条件，阅读成本高，不直观。
- 影响：维护困难，后续调参容易误判逻辑关系。
- 当前判断：中高优先级问题。
- 后续方向：后续考虑拆成“门禁 -> 幅度类分流 -> 常包络类分流 -> 细分类”的分层函数结构。

### A9. 当前分类顺序仍偏工程拼接
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：当前顺序为 `NO_CARRIER -> ASK -> AM -> PSK -> FSK -> FM -> CW`。
- 影响：FM 更像剩余桶，FSK 过度依赖 `single_sign`，PSK 依赖粗跳变特征。
- 当前判断：高优先级问题。
- 后续方向：后续重构为：`carrier/no-carrier -> 幅度类/常包络类 -> CW/FM/FSK/PSK`。

### A10. 各类调制速率估计普遍偏低
- 位置：`Core/App/Signal/app_mod_detect.c`
- 现象：当前 AM / ASK / FM / FSK / PSK 的调制速率显示整体偏低。
- 影响：参数显示可看趋势，但绝对值可信度不足。
- 当前判断：高优先级问题。
- 后续方向：需要重新审查速率估计链路，包括边沿计数、符号切换计数、相位跳变计数，以及各模式下的经验缩放系数。

## B. 工程与架构问题

### B1. `DTCMRAM` 仍然偏高
- 现象：当前约 82.5%。
- 影响：可运行，但后续再加大缓冲时容易再次顶高。
- 当前判断：中优先级风险。
- 后续方向：新增 FFT 缓冲、历史缓冲、频谱缓存时，优先放 `RAM` 或 `RAM_D2`，不要默认进 `.bss`。

### B2. 调试代码仍在工程内，仅默认关闭
- 位置：`AdcTask.c`、`app_mod_detect.c` 等
- 现象：`adc_range`、`adc_log_algo_1s`、`adc_log_health_1s`、`mod/ui trace` 代码仍存在。
- 影响：当前不影响运行，但后续若开关失控会再次增加实时路径负担。
- 当前判断：中优先级问题。
- 后续方向：后续继续收口，必要时再拆分为专门调试模块。

### B3. `ModDetectTask` 统计未对外展示
- 位置：`ModDetectTask.c`
- 现象：已有 `submit_ok_cnt`、`submit_drop_cnt`、`queue_full_drop_cnt`、`process_cnt` 等统计，但默认未对外展示。
- 影响：后续若出现掉块，不容易第一时间发现。
- 当前判断：中优先级问题。
- 后续方向：后续可考虑增加轻量健康状态读取接口，但当前不急着改。

### B4. 工程内历史 `.bak_*` 文件较多
- 现象：活跃目录存在较多历史备份文件。
- 影响：阅读和维护时容易混淆。
- 当前判断：低到中优先级问题。
- 后续方向：当前阶段不删除；后续若清理，需先统一备份策略和归档位置。

## C. 当前共识
- 现在先不修改上述问题。
- 先继续把现有算法、链路、UI 数据流彻底看懂。
- 真正开始处理时，优先级建议：
  1. A2 / A6 包络极值污染问题
  2. A4 / A5 PSK 相位特征过粗问题
  3. A8 / A9 分类树重构与可读性提升
  4. A1 性能优化
  5. B 类工程清理


